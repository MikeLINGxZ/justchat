# 主 Agent 优化

现在，主 agent 的需求已经实现完成了，为了改进主 agent 的使用体验，需要对一些地方进行优化。

**优化点：**

1. 当用户点击新建对话的时候，左侧聊天列表中不需要添加一个新项，当用户发送了第一条消息后，才新建
2. 当大模型使用文件读取、代码执行工具的时候，需要向用户确认（这个可以借助 trpc-agent-go 的能力实现？）
3. 涉及工具调用的时候，工具调用信息需要在思考下方，主内容上方
4. 当我点击一个对话的时候，进入对话时会看到滚动到底部的动画，可否优化成直接显示在底部
