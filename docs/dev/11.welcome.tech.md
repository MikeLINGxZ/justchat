# 欢迎向导（Onboarding）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现首次启动引导：四步向导（欢迎 → 选择供应商 → 配置供应商 → Node.js 运行时下载），并联动后端 onboarding / runtime 服务。

**Architecture:** 独立 `entry=onboarding` 窗口，与主窗口互斥呈现；后端新增 `onboarding`、`runtime` 两个服务；前端复用 `AddProviderStepForm`，新增 `components/onboarding/*` 组件树；初始化标记落地为 `init.json`，Node 运行时落地为 `runtime/node/state.json`。

**Tech Stack:** Wails v3 + Go + React + Tailwind + i18next。

参考需求文档：`docs/dev/11.welcome.md`。所有实现必须遵循 `docs/dev/00.rules.md`。

---

## 文件结构

### 新建文件

```
backend/
  service/
    onboarding/
      onboarding.go
      onboarding_implement.go
      onboarding_internal.go
      onboarding_test.go
      onboarding_dto/
        is_initialized.go
        save_provider_and_mark_initialized.go
        enter_home.go
        exit_app.go
    runtime/
      runtime.go
      runtime_implement.go
      runtime_internal.go
      runtime_constants.go
      runtime_test.go
      runtime_dto/
        get_status.go
        download_node.go
        cancel_download.go
        mark_download_later.go
  pkg/
    window_options/
      window_options.go

frontend/
  src/
    components/
      onboarding/
        OnboardingApp.tsx
        Stepper.tsx
        OnboardingStepIntro.tsx
        OnboardingStepSelect.tsx
        OnboardingStepForm.tsx
        OnboardingStepRuntime.tsx
      settings/
        general/
          NodeRuntimeSection.tsx       # 若 general 目录不存在则一并新建
```

### 修改文件

```
backend/
  pkg/
    id/window_id/window_id.go          # 新增 Onboarding 常量
    ierror/ierrors.go                  # 新增 onboarding 与 runtime 错误码
    i18n/resources_en_zh_cn.go         # 新增中文错误文案
    i18n/resources_en_us.go            # 新增英文错误文案
  service/
    window/window.go                   # 新增 OpenOnboarding
    window/window_dto/                 # 新增 open_onboarding.go
main.go                                # 启动时根据 IsInitialized 分流

frontend/
  src/
    App.tsx                            # 新增 entry === 'onboarding' 分支
    components/settings/providers/
      AddProviderStepForm.tsx          # 增加 mode prop
    i18n/locales/zh-CN.ts              # 新增 onboarding.*、runtime.*
    i18n/locales/en.ts                 # 新增 onboarding.*、runtime.*
```

---

## 实施约束

- **每个 Go 公开方法都加注释**（项目规范）
- **公开方法返回 `ierror.Error(code, err)` 包装错误**
- **前端禁止 `any`，所有字段显式类型**
- **不在已有大组件中嵌入新逻辑，按文件清单新建组件**
- **每个任务跑通后立即 commit**，commit 不带 Claude 模型署名
- 使用 `cnpm` 而非 `npm` 安装前端依赖

---

## Task 1: 新增 Onboarding 窗口 ID 常量

**Files:**
- Modify: `backend/pkg/id/window_id/window_id.go`

- [ ] **Step 1: 修改 window_id.go**

替换文件内容为：

```go
package window_id

const Home = "window_home"
const Settings = "window_settings"
const AddProvider = "window_add_provider"
const Onboarding = "window_onboarding"
```

- [ ] **Step 2: 编译通过**

```bash
go build ./...
```

期望：无错误输出。

- [ ] **Step 3: Commit**

```bash
git add backend/pkg/id/window_id/window_id.go
git commit -m "feat(window): add onboarding window id constant"
```

---

## Task 2: 抽取 defaultWindowOptions 到独立包

**Files:**
- Create: `backend/pkg/window_options/window_options.go`
- Modify: `main.go`

**说明：** 后续 onboarding 服务的 `EnterHome` 需要在 main 包之外创建主窗口，必须把 `defaultWindowOptions` 抽到独立包避免循环依赖。

- [ ] **Step 1: 创建 backend/pkg/window_options/window_options.go**

```go
package window_options

import (
	"github.com/wailsapp/wails/v3/pkg/application"
)

// DefaultHome returns the default WebviewWindowOptions used by the application home window.
func DefaultHome() application.WebviewWindowOptions {
	return application.WebviewWindowOptions{
		Title:          "lemontea",
		EnableFileDrop: true,
		Mac: application.MacWindow{
			InvisibleTitleBarHeight: 48,
			Backdrop:                application.MacBackdropTranslucent,
			TitleBar:                application.MacTitleBarHiddenInsetUnified,
		},
		BackgroundColour: application.NewRGB(27, 38, 54),
		URL:              "/",
		Width:            1580,
		Height:           980,
		MinWidth:         550,
		MinHeight:        550,
	}
}

// DefaultOnboarding returns the WebviewWindowOptions for the first-launch onboarding window.
func DefaultOnboarding() application.WebviewWindowOptions {
	return application.WebviewWindowOptions{
		Title:          "lemontea",
		EnableFileDrop: false,
		Mac: application.MacWindow{
			InvisibleTitleBarHeight: 48,
			Backdrop:                application.MacBackdropTranslucent,
			TitleBar:                application.MacTitleBarHiddenInsetUnified,
		},
		BackgroundColour: application.NewRGB(27, 38, 54),
		URL:              "/?entry=onboarding",
		Width:            1140,
		Height:           676,
		MinWidth:         720,
		MinHeight:        560,
	}
}
```

- [ ] **Step 2: 修改 main.go 引用 window_options.DefaultHome()**

将 `main.go` 中：

```go
func defaultWindowOptions() application.WebviewWindowOptions { ... }
```

整段删除。`main` 函数里：

```go
win := app.Window.NewWithOptions(defaultWindowOptions())
```

改为：

```go
win := app.Window.NewWithOptions(window_options.DefaultHome())
```

import 块新增：

```go
"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/window_options"
```

- [ ] **Step 3: 编译并启动验证**

```bash
go build ./...
```

期望：无错误。然后跑：

```bash
task dev
```

期望：主窗口正常打开，行为与之前一致。手动关闭。

- [ ] **Step 4: Commit**

```bash
git add backend/pkg/window_options/window_options.go main.go
git commit -m "refactor: extract window options into reusable package"
```

---

## Task 3: 创建 onboarding 服务骨架 + IsInitialized + 单测

**Files:**
- Create: `backend/service/onboarding/onboarding.go`
- Create: `backend/service/onboarding/onboarding_implement.go`
- Create: `backend/service/onboarding/onboarding_internal.go`
- Create: `backend/service/onboarding/onboarding_dto/is_initialized.go`
- Create: `backend/service/onboarding/onboarding_test.go`
- Modify: `backend/pkg/ierror/ierrors.go`
- Modify: `backend/pkg/i18n/resources_en_zh_cn.go`
- Modify: `backend/pkg/i18n/resources_en_us.go`

- [ ] **Step 1: 添加错误码**

在 `backend/pkg/ierror/ierrors.go` 的 const 块末尾追加：

```go
	// Onboarding error codes
	ErrOnboardingReadInit  errorCode = "ierror.onboarding.read_init"
	ErrOnboardingWriteInit errorCode = "ierror.onboarding.write_init"
	ErrOnboardingComplete  errorCode = "ierror.onboarding.complete"
```

在 `backend/pkg/i18n/resources_en_zh_cn.go` 末尾的 map 中追加：

```go
	"ierror.onboarding.read_init":  "读取初始化状态失败",
	"ierror.onboarding.write_init": "写入初始化状态失败",
	"ierror.onboarding.complete":   "完成初始化失败",
```

在 `backend/pkg/i18n/resources_en_us.go` 对应位置追加：

```go
	"ierror.onboarding.read_init":  "Failed to read initialization state",
	"ierror.onboarding.write_init": "Failed to write initialization state",
	"ierror.onboarding.complete":   "Failed to complete initialization",
```

- [ ] **Step 2: 创建 onboarding_dto/is_initialized.go**

```go
package onboarding_dto

// IsInitializedInput is the empty input for IsInitialized.
type IsInitializedInput struct{}

// IsInitializedOutput carries whether the application has finished onboarding.
type IsInitializedOutput struct {
	Initialized bool `json:"initialized"`
}
```

- [ ] **Step 3: 创建 onboarding_internal.go**

```go
package onboarding

import (
	"encoding/json"
	"os"
	"path/filepath"
	"time"

	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/dir"
)

const initFileName = "init.json"

type initState struct {
	Initialized bool      `json:"initialized"`
	CompletedAt time.Time `json:"completed_at"`
}

// initFilePath returns the absolute path to the init marker file under the data dir.
func initFilePath() (string, error) {
	dataDir, err := dir.GetDataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dataDir, initFileName), nil
}

// isInitialized reports whether the init.json marker file exists.
func isInitialized() (bool, error) {
	path, err := initFilePath()
	if err != nil {
		return false, err
	}
	if _, err := os.Stat(path); err != nil {
		if os.IsNotExist(err) {
			return false, nil
		}
		return false, err
	}
	return true, nil
}

// markInitialized writes the init.json marker file containing the completion timestamp.
func markInitialized() error {
	path, err := initFilePath()
	if err != nil {
		return err
	}
	payload, err := json.MarshalIndent(initState{
		Initialized: true,
		CompletedAt: time.Now(),
	}, "", "  ")
	if err != nil {
		return err
	}
	return os.WriteFile(path, payload, 0o644)
}
```

- [ ] **Step 4: 创建 onboarding_implement.go**

```go
package onboarding

import (
	"context"

	"github.com/wailsapp/wails/v3/pkg/application"
)

// ServiceStartup captures the Wails application handle once the service is registered.
func (s *Onboarding) ServiceStartup(ctx context.Context, options application.ServiceOptions) error {
	s.wailsApp = application.Get()
	return nil
}
```

- [ ] **Step 5: 创建 onboarding.go 骨架与 IsInitialized**

```go
package onboarding

import (
	"context"

	"github.com/wailsapp/wails/v3/pkg/application"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/ierror"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding/onboarding_dto"
)

// Onboarding manages the first-launch initialization flow.
type Onboarding struct {
	wailsApp *application.App
}

// NewOnboarding constructs a new Onboarding service.
func NewOnboarding() *Onboarding {
	return &Onboarding{}
}

// IsInitialized reports whether the application has completed first-launch onboarding.
func (s *Onboarding) IsInitialized(ctx context.Context, input onboarding_dto.IsInitializedInput) (*onboarding_dto.IsInitializedOutput, error) {
	initialized, err := isInitialized()
	if err != nil {
		return nil, ierror.Error(ierror.ErrOnboardingReadInit, err)
	}
	return &onboarding_dto.IsInitializedOutput{Initialized: initialized}, nil
}
```

- [ ] **Step 6: 写失败的测试**

创建 `backend/service/onboarding/onboarding_test.go`：

```go
package onboarding

import (
	"os"
	"path/filepath"
	"testing"
)

// withTempDataDir overrides LEMONTEA_DATA_DIR for the test scope.
func withTempDataDir(t *testing.T) string {
	t.Helper()
	tmp := t.TempDir()
	t.Setenv("LEMONTEA_DATA_DIR", tmp)
	return tmp
}

func TestIsInitialized_FalseWhenNoFile(t *testing.T) {
	withTempDataDir(t)

	got, err := isInitialized()
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if got {
		t.Fatalf("expected false, got true")
	}
}

func TestMarkInitialized_WritesFile(t *testing.T) {
	tmp := withTempDataDir(t)

	if err := markInitialized(); err != nil {
		t.Fatalf("markInitialized error: %v", err)
	}
	if _, err := os.Stat(filepath.Join(tmp, "init.json")); err != nil {
		t.Fatalf("init.json missing: %v", err)
	}

	got, err := isInitialized()
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if !got {
		t.Fatalf("expected true after mark, got false")
	}
}

func TestMarkInitialized_Idempotent(t *testing.T) {
	withTempDataDir(t)
	if err := markInitialized(); err != nil {
		t.Fatalf("first mark: %v", err)
	}
	if err := markInitialized(); err != nil {
		t.Fatalf("second mark: %v", err)
	}
	got, err := isInitialized()
	if err != nil {
		t.Fatalf("unexpected error: %v", err)
	}
	if !got {
		t.Fatalf("expected true, got false")
	}
}
```

- [ ] **Step 7: 跑测试**

```bash
go test ./backend/service/onboarding/...
```

期望：3 个测试均 PASS。

- [ ] **Step 8: Commit**

```bash
git add backend/service/onboarding/ backend/pkg/ierror/ierrors.go backend/pkg/i18n/
git commit -m "feat(onboarding): add service skeleton with IsInitialized and init.json tracking"
```

---

## Task 4: SaveProviderAndMarkInitialized

**Files:**
- Create: `backend/service/onboarding/onboarding_dto/save_provider_and_mark_initialized.go`
- Modify: `backend/service/onboarding/onboarding.go`
- Modify: `backend/service/onboarding/onboarding_test.go`

- [ ] **Step 1: 创建 DTO**

`backend/service/onboarding/onboarding_dto/save_provider_and_mark_initialized.go`：

```go
package onboarding_dto

import (
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/models/view_model"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/provider"
)

// SaveProviderAndMarkInitializedInput mirrors provider_dto.CreateProviderInput plus onboarding intent.
type SaveProviderAndMarkInitializedInput struct {
	ProviderName string             `json:"provider_name"`
	ProviderType provider.Type      `json:"provider_type"`
	BaseUrl      string             `json:"base_url"`
	ApiKey       string             `json:"api_key"`
	Enable       bool               `json:"enable"`
	DefaultModel *string            `json:"default_model"`
	Models       []view_model.Model `json:"models"`
}

// SaveProviderAndMarkInitializedOutput is an empty acknowledgement.
type SaveProviderAndMarkInitializedOutput struct{}
```

- [ ] **Step 2: 修改 onboarding.go 添加方法**

在 `Onboarding` 结构体中新增字段 `providerSvc *provider.Provider`；构造函数 `NewOnboarding` 改为：

```go
package onboarding

import (
	"context"

	"github.com/wailsapp/wails/v3/pkg/application"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/ierror"
	providerSvc "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/provider"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/provider/provider_dto"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding/onboarding_dto"
)

// Onboarding manages the first-launch initialization flow.
type Onboarding struct {
	wailsApp    *application.App
	providerSvc *providerSvc.Provider
}

// NewOnboarding constructs a new Onboarding service bound to the provider service.
func NewOnboarding(p *providerSvc.Provider) *Onboarding {
	return &Onboarding{providerSvc: p}
}

// IsInitialized reports whether the application has completed first-launch onboarding.
func (s *Onboarding) IsInitialized(ctx context.Context, input onboarding_dto.IsInitializedInput) (*onboarding_dto.IsInitializedOutput, error) {
	initialized, err := isInitialized()
	if err != nil {
		return nil, ierror.Error(ierror.ErrOnboardingReadInit, err)
	}
	return &onboarding_dto.IsInitializedOutput{Initialized: initialized}, nil
}

// SaveProviderAndMarkInitialized creates the first provider and writes the init.json marker atomically from the caller's perspective.
func (s *Onboarding) SaveProviderAndMarkInitialized(ctx context.Context, input onboarding_dto.SaveProviderAndMarkInitializedInput) (*onboarding_dto.SaveProviderAndMarkInitializedOutput, error) {
	if _, err := s.providerSvc.CreateProvider(ctx, provider_dto.CreateProviderInput{
		ProviderName: input.ProviderName,
		ProviderType: input.ProviderType,
		BaseUrl:      input.BaseUrl,
		ApiKey:       input.ApiKey,
		Enable:       input.Enable,
		DefaultModel: input.DefaultModel,
		Models:       input.Models,
	}); err != nil {
		return nil, err
	}

	if err := markInitialized(); err != nil {
		return nil, ierror.Error(ierror.ErrOnboardingWriteInit, err)
	}

	return &onboarding_dto.SaveProviderAndMarkInitializedOutput{}, nil
}
```

> 注意：以 `provider_dto.CreateProviderInput` 的实际字段为准；如有不一致字段，按当前项目的 DTO 调整。

- [ ] **Step 3: 编译通过**

```bash
go build ./...
```

期望：无错误。

- [ ] **Step 4: 跑现有测试**

```bash
go test ./backend/service/onboarding/...
```

期望：原有三个测试仍 PASS。

- [ ] **Step 5: Commit**

```bash
git add backend/service/onboarding/
git commit -m "feat(onboarding): add SaveProviderAndMarkInitialized binding"
```

---

## Task 5: ExitApp 与 EnterHome

**Files:**
- Create: `backend/service/onboarding/onboarding_dto/exit_app.go`
- Create: `backend/service/onboarding/onboarding_dto/enter_home.go`
- Modify: `backend/service/onboarding/onboarding.go`

- [ ] **Step 1: 创建 DTO**

`onboarding_dto/exit_app.go`：

```go
package onboarding_dto

type ExitAppInput struct{}
type ExitAppOutput struct{}
```

`onboarding_dto/enter_home.go`：

```go
package onboarding_dto

type EnterHomeInput struct{}
type EnterHomeOutput struct{}
```

- [ ] **Step 2: 添加方法到 onboarding.go**

import 块追加：

```go
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/id/window_id"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/window_options"
```

在文件末尾追加两个方法：

```go
// ExitApp terminates the Wails application; the OS-level process exits.
func (s *Onboarding) ExitApp(ctx context.Context, input onboarding_dto.ExitAppInput) (*onboarding_dto.ExitAppOutput, error) {
	if s.wailsApp != nil {
		s.wailsApp.Quit()
	}
	return &onboarding_dto.ExitAppOutput{}, nil
}

// EnterHome opens the main window and closes the onboarding window.
func (s *Onboarding) EnterHome(ctx context.Context, input onboarding_dto.EnterHomeInput) (*onboarding_dto.EnterHomeOutput, error) {
	if s.wailsApp == nil {
		return &onboarding_dto.EnterHomeOutput{}, nil
	}

	homeWindow, ok := s.wailsApp.Window.GetByName(window_id.Home)
	if !ok {
		opts := window_options.DefaultHome()
		opts.Name = window_id.Home
		homeWindow = s.wailsApp.Window.NewWithOptions(opts)
	}
	homeWindow.Focus()
	homeWindow.Show()

	if onboardWin, ok := s.wailsApp.Window.GetByName(window_id.Onboarding); ok {
		onboardWin.Close()
	}

	return &onboarding_dto.EnterHomeOutput{}, nil
}
```

- [ ] **Step 3: 编译通过**

```bash
go build ./...
```

期望：无错误。

- [ ] **Step 4: Commit**

```bash
git add backend/service/onboarding/
git commit -m "feat(onboarding): add ExitApp and EnterHome bindings"
```

---

## Task 6: window 服务的 OpenOnboarding

**Files:**
- Create: `backend/service/window/window_dto/open_onboarding.go`
- Modify: `backend/service/window/window.go`

- [ ] **Step 1: 创建 DTO**

`backend/service/window/window_dto/open_onboarding.go`：

```go
package window_dto

type OpenOnboardingInput struct{}
type OpenOnboardingOutput struct{}
```

- [ ] **Step 2: 添加 OpenOnboarding 方法**

`backend/service/window/window.go` import 块追加：

```go
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/window_options"
```

在文件末尾追加：

```go
// OpenOnboarding opens or focuses the first-launch onboarding window.
func (p *Window) OpenOnboarding(ctx context.Context, input window_dto.OpenOnboardingInput) (*window_dto.OpenOnboardingOutput, error) {
	if win, ok := p.wailsApp.Window.GetByName(window_id.Onboarding); ok {
		win.Focus()
		p.centerWindowOnHomeScreen(win)
		win.Show()
		return &window_dto.OpenOnboardingOutput{}, nil
	}

	opts := window_options.DefaultOnboarding()
	opts.Name = window_id.Onboarding
	win := p.wailsApp.Window.NewWithOptions(opts)
	win.Focus()
	p.centerWindowOnHomeScreen(win)
	win.Show()
	return &window_dto.OpenOnboardingOutput{}, nil
}
```

- [ ] **Step 3: 编译通过**

```bash
go build ./...
```

期望：无错误。

- [ ] **Step 4: Commit**

```bash
git add backend/service/window/
git commit -m "feat(window): add OpenOnboarding binding"
```

---

## Task 7: main.go 启动分流

**Files:**
- Modify: `main.go`

- [ ] **Step 1: 替换 main 函数核心逻辑**

import 块追加：

```go
	"context"
	onboardingSvc "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding/onboarding_dto"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/id/window_id"
```

`main` 函数中（位于 `storage.NewStorage()` 之后、`app.Run()` 之前）：

```go
	providerService := provider.NewProvider(istorage)
	onboardingService := onboardingSvc.NewOnboarding(providerService)

	app := application.New(application.Options{
		Name:        window_id.Home,
		Description: "A demo of using raw HTML & CSS",
		Services: []application.Service{
			application.NewService(&settings.Settings{}),
			application.NewService(agentSvc.NewAgent(istorage)),
			application.NewService(providerService),
			application.NewService(onboardingService),
			application.NewService(pluginSvc.NewPlugin()),
			application.NewService(&process.Process{}),
			application.NewService(&window.Window{}),
			application.NewService(&file.File{}),
			application.NewService(&config.Config{}),
		},
		Assets: application.AssetOptions{
			Handler: application.AssetFileServerFS(assets),
		},
		Mac: application.MacOptions{
			ApplicationShouldTerminateAfterLastWindowClosed: true,
		},
	})

	initState, err := onboardingService.IsInitialized(context.Background(), onboarding_dto.IsInitializedInput{})
	if err != nil {
		log.Fatal(err)
	}

	if initState != nil && initState.Initialized {
		opts := window_options.DefaultHome()
		opts.Name = window_id.Home
		win := app.Window.NewWithOptions(opts)
		win.OnWindowEvent(events.Common.WindowFilesDropped, func(event *application.WindowEvent) {
			application.Get().Event.Emit("files-dropped", map[string]any{
				"files": event.Context().DroppedFiles(),
			})
		})
	} else {
		opts := window_options.DefaultOnboarding()
		opts.Name = window_id.Onboarding
		app.Window.NewWithOptions(opts)
	}

	if err := app.Run(); err != nil {
		log.Fatal(err)
	}
```

旧的内联服务列表和 `win := ...` 全部替换。

- [ ] **Step 2: 编译并启动验证**

```bash
go build ./...
```

期望：无错误。然后清理 init.json（如果存在）以模拟首次启动：

```bash
rm -f ~/.lemontea/init.json
```

启动：

```bash
task dev
```

期望：onboarding 窗口打开（页面暂时空白或 404，这是正常的，前端尚未实现）；进程不崩溃。手动关闭。

- [ ] **Step 3: Commit**

```bash
git add main.go
git commit -m "feat(app): branch startup between onboarding and home windows"
```

---

## Task 8: 前端入口分发 + OnboardingApp 骨架

**Files:**
- Modify: `frontend/src/App.tsx`
- Create: `frontend/src/components/onboarding/OnboardingApp.tsx`

- [ ] **Step 1: 创建 OnboardingApp.tsx 占位**

```tsx
import { useState } from 'react'
import { AppSettingsSyncProvider } from '@/components/providers/AppSettingsSyncProvider'
import { AlertEventProvider } from '@/components/providers/AlertEventProvider'
import { ThemeProvider } from '@/components/providers/ThemeProvider'
import { FontSizeProvider } from '@/components/providers/FontSizeProvider'
import { AlertViewport } from '@/components/alert/AlertViewport'

export type OnboardingStep = 'intro' | 'select' | 'config' | 'runtime'

export function OnboardingApp() {
  return (
    <AppSettingsSyncProvider>
      <AlertEventProvider>
        <ThemeProvider>
          <FontSizeProvider>
            <OnboardingWizard />
            <AlertViewport />
          </FontSizeProvider>
        </ThemeProvider>
      </AlertEventProvider>
    </AppSettingsSyncProvider>
  )
}

function OnboardingWizard() {
  const [step, setStep] = useState<OnboardingStep>('intro')

  return (
    <div className="flex h-screen flex-col overflow-hidden bg-background text-foreground">
      <div className="p-6 text-sm text-muted-foreground">
        Onboarding step: {step}
      </div>
      <div className="flex gap-2 p-6">
        <button onClick={() => setStep('intro')} className="rounded border px-3 py-1">intro</button>
        <button onClick={() => setStep('select')} className="rounded border px-3 py-1">select</button>
        <button onClick={() => setStep('config')} className="rounded border px-3 py-1">config</button>
        <button onClick={() => setStep('runtime')} className="rounded border px-3 py-1">runtime</button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 修改 App.tsx 添加分支**

把 `App.tsx` 的入口判断改为：

```tsx
import { AppSettingsSyncProvider } from '@/components/providers/AppSettingsSyncProvider'
import { AlertEventProvider } from '@/components/providers/AlertEventProvider'
import { ThemeProvider } from '@/components/providers/ThemeProvider'
import { FontSizeProvider } from '@/components/providers/FontSizeProvider'
import { MainLayout } from '@/components/layout/MainLayout'
import { SettingsApp } from '@/components/settings/SettingsApp'
import { AddProviderApp } from '@/components/settings/providers/AddProviderApp'
import { OnboardingApp } from '@/components/onboarding/OnboardingApp'
import { AlertViewport } from '@/components/alert/AlertViewport'

function App() {
  const params = new URLSearchParams(window.location.search)
  const entry = params.get('entry')

  if (entry === 'onboarding') {
    return <OnboardingApp />
  }

  return (
    <AppSettingsSyncProvider>
      <AlertEventProvider>
        <ThemeProvider>
          <FontSizeProvider>
            {entry === 'settings'
              ? <SettingsApp />
              : entry === 'add_provider'
                ? <AddProviderApp />
                : <MainLayout />}
            <AlertViewport />
          </FontSizeProvider>
        </ThemeProvider>
      </AlertEventProvider>
    </AppSettingsSyncProvider>
  )
}

export default App
```

> `OnboardingApp` 自带 Providers，所以放在 `App` 的外层条件之上。

- [ ] **Step 3: 启动验证**

清理标记并启动：

```bash
rm -f ~/.lemontea/init.json
task dev
```

期望：onboarding 窗口显示 `Onboarding step: intro` 与 4 个切换按钮，点按钮可切换文案。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/App.tsx frontend/src/components/onboarding/OnboardingApp.tsx
git commit -m "feat(onboarding): scaffold OnboardingApp entry routing"
```

---

## Task 9: Stepper 组件

**Files:**
- Create: `frontend/src/components/onboarding/Stepper.tsx`
- Modify: `frontend/src/components/onboarding/OnboardingApp.tsx`

- [ ] **Step 1: 创建 Stepper.tsx**

```tsx
import { useTranslation } from 'react-i18next'
import { cn } from '@/lib/utils'
import type { OnboardingStep } from './OnboardingApp'

type Props = {
  current: OnboardingStep
}

const order: OnboardingStep[] = ['intro', 'select', 'config', 'runtime']

const labelKeys: Record<OnboardingStep, string> = {
  intro: 'onboarding.steps.intro',
  select: 'onboarding.steps.select',
  config: 'onboarding.steps.config',
  runtime: 'onboarding.steps.runtime',
}

export function Stepper(props: Props) {
  const { t } = useTranslation()
  const currentIdx = order.indexOf(props.current)

  return (
    <div className="flex items-center gap-2">
      {order.map((step, idx) => {
        const active = idx === currentIdx
        const done = idx < currentIdx
        return (
          <div key={step} className="flex items-center gap-2">
            {idx > 0 && <div className="h-px w-8 bg-border" />}
            <div className="flex items-center gap-1.5">
              <div
                className={cn(
                  'flex h-5 w-5 items-center justify-center rounded-full text-xs font-semibold',
                  active
                    ? 'bg-primary text-primary-foreground'
                    : done
                      ? 'bg-primary/30 text-primary'
                      : 'bg-muted text-muted-foreground',
                )}
              >
                {idx + 1}
              </div>
              <span
                className={cn(
                  'text-xs',
                  active ? 'font-medium text-foreground' : 'text-muted-foreground',
                )}
              >
                {t(labelKeys[step])}
              </span>
            </div>
          </div>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 2: 在 OnboardingApp 接入 Stepper**

将 `OnboardingWizard` 中调试用的 placeholder 替换为：

```tsx
function OnboardingWizard() {
  const [step, setStep] = useState<OnboardingStep>('intro')

  return (
    <div className="flex h-screen flex-col overflow-hidden bg-background text-foreground">
      <div className="shrink-0 px-6 pb-3 pt-12">
        <Stepper current={step} />
      </div>
      <div className="flex min-h-0 flex-1 flex-col overflow-hidden px-6 pb-6">
        {/* steps will be plugged in subsequent tasks */}
        <div className="text-sm text-muted-foreground">Step: {step}</div>
        <div className="mt-4 flex gap-2">
          <button onClick={() => setStep('intro')} className="rounded border px-3 py-1">intro</button>
          <button onClick={() => setStep('select')} className="rounded border px-3 py-1">select</button>
          <button onClick={() => setStep('config')} className="rounded border px-3 py-1">config</button>
          <button onClick={() => setStep('runtime')} className="rounded border px-3 py-1">runtime</button>
        </div>
      </div>
    </div>
  )
}
```

import 块新增：

```tsx
import { Stepper } from './Stepper'
```

- [ ] **Step 3: 启动验证**

```bash
task dev
```

期望：顶部 stepper 显示 4 个圆点，当前步圆点高亮。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/onboarding/
git commit -m "feat(onboarding): add Stepper component"
```

---

## Task 10: i18n onboarding 命名空间

**Files:**
- Modify: `frontend/src/i18n/locales/zh-CN.ts`
- Modify: `frontend/src/i18n/locales/en.ts`

- [ ] **Step 1: 在 zh-CN.ts 顶层对象插入 onboarding 命名空间**

参考现有结构（例如 `settingsPage` 命名空间）的位置追加：

```ts
  onboarding: {
    steps: {
      intro: '欢迎',
      select: '选择供应商',
      config: '配置供应商',
      runtime: 'Node.js 运行时',
    },
    intro: {
      badge: '开始使用',
      heroTitle: '先连接第一个模型供应商，再开始你的第一段对话',
      cards: {
        whatTitle: '这是什么',
        whatText: 'Lemontea 帮你管理与大模型供应商的连接，并提供本地对话与插件能力。',
        whyTitle: '为什么先连',
        whyText: '需要至少一个可用的模型供应商，软件才能与大模型对话。',
        laterTitle: '以后还能改',
        laterText: '可以随时在「设置 - 供应商」里调整、删除或新增更多供应商。',
      },
    },
    actions: {
      exit: '退出',
      start: '开始配置',
      back: '返回',
      next: '下一步',
      enterHome: '进入主界面',
      downloadNow: '立即下载',
      downloadLater: '稍后下载',
      retry: '重试',
      skip: '跳过',
      cancel: '取消',
    },
    select: {
      hint: '选择一个你计划接入的模型供应商',
      loadFailed: '加载供应商列表失败',
      empty: '没有可用的供应商',
    },
    config: {
      title: '配置 {{name}}',
      hintFallback: '请填写访问该供应商所需的信息',
      saveFailed: '保存供应商配置失败',
    },
    runtime: {
      title: 'Node.js 运行时',
      intro: '部分插件需要本地 Node.js 运行时支持，可以现在下载，也可以稍后在设置里下载。',
      stateMissing: '尚未安装',
      stateDownloading: '下载中…',
      stateReady: '已安装',
      statePendingLater: '已选择稍后下载',
      stateFailed: '下载失败',
      phaseDownload: '下载',
      phaseExtract: '解压',
      phaseVerify: '校验',
    },
    loadFailed: '初始化页面加载失败',
  },
```

- [ ] **Step 2: 在 en.ts 顶层对象插入对应英文翻译**

```ts
  onboarding: {
    steps: {
      intro: 'Welcome',
      select: 'Select provider',
      config: 'Configure provider',
      runtime: 'Node.js runtime',
    },
    intro: {
      badge: 'Get started',
      heroTitle: 'Connect your first model provider to start your first conversation',
      cards: {
        whatTitle: 'What is this',
        whatText: 'Lemontea manages your connections to LLM providers and provides local chat and plugins.',
        whyTitle: 'Why connect now',
        whyText: 'At least one usable model provider is required to chat with an LLM.',
        laterTitle: 'You can change it later',
        laterText: 'Adjust, remove, or add more providers anytime in Settings → Providers.',
      },
    },
    actions: {
      exit: 'Exit',
      start: 'Start setup',
      back: 'Back',
      next: 'Next',
      enterHome: 'Enter app',
      downloadNow: 'Download now',
      downloadLater: 'Download later',
      retry: 'Retry',
      skip: 'Skip',
      cancel: 'Cancel',
    },
    select: {
      hint: 'Choose a model provider to connect',
      loadFailed: 'Failed to load providers',
      empty: 'No providers available',
    },
    config: {
      title: 'Configure {{name}}',
      hintFallback: 'Fill in the credentials required for this provider',
      saveFailed: 'Failed to save provider',
    },
    runtime: {
      title: 'Node.js runtime',
      intro: 'Some plugins require a local Node.js runtime. Download now, or do it later from Settings.',
      stateMissing: 'Not installed',
      stateDownloading: 'Downloading…',
      stateReady: 'Installed',
      statePendingLater: 'Deferred',
      stateFailed: 'Download failed',
      phaseDownload: 'Download',
      phaseExtract: 'Extract',
      phaseVerify: 'Verify',
    },
    loadFailed: 'Failed to load onboarding',
  },
```

- [ ] **Step 3: 启动验证**

```bash
task dev
```

期望：Stepper 文案变成"欢迎 / 选择供应商 / 配置供应商 / Node.js 运行时"。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/i18n/locales/
git commit -m "feat(onboarding): add i18n strings for onboarding wizard"
```

---

## Task 11: OnboardingStepIntro

**Files:**
- Create: `frontend/src/components/onboarding/OnboardingStepIntro.tsx`
- Modify: `frontend/src/components/onboarding/OnboardingApp.tsx`

- [ ] **Step 1: 创建 OnboardingStepIntro.tsx**

```tsx
import { useTranslation } from 'react-i18next'
import { Onboarding } from '@bindings/gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding'

type Props = {
  onStart: () => void
}

export function OnboardingStepIntro(props: Props) {
  const { t } = useTranslation()

  const handleExit = () => {
    void Onboarding.ExitApp({})
  }

  return (
    <div className="flex h-full min-h-0 flex-col gap-6">
      <div className="space-y-4">
        <span className="inline-block rounded-full bg-primary/10 px-3 py-1 text-xs font-semibold uppercase tracking-wider text-primary">
          {t('onboarding.intro.badge')}
        </span>
        <h1 className="text-3xl font-semibold leading-tight text-foreground">
          {t('onboarding.intro.heroTitle')}
        </h1>
      </div>

      <div className="grid gap-3 sm:grid-cols-3">
        <IntroCard titleKey="onboarding.intro.cards.whatTitle" textKey="onboarding.intro.cards.whatText" />
        <IntroCard titleKey="onboarding.intro.cards.whyTitle" textKey="onboarding.intro.cards.whyText" />
        <IntroCard titleKey="onboarding.intro.cards.laterTitle" textKey="onboarding.intro.cards.laterText" />
      </div>

      <div className="mt-auto flex justify-end gap-2">
        <button
          type="button"
          onClick={handleExit}
          className="rounded-xl border border-border px-5 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent"
        >
          {t('onboarding.actions.exit')}
        </button>
        <button
          type="button"
          onClick={props.onStart}
          className="rounded-xl bg-primary px-5 py-2 text-sm font-medium text-primary-foreground"
        >
          {t('onboarding.actions.start')}
        </button>
      </div>
    </div>
  )
}

function IntroCard(props: { titleKey: string; textKey: string }) {
  const { t } = useTranslation()
  return (
    <div className="rounded-2xl border border-border bg-card p-4">
      <div className="text-sm font-semibold text-primary">{t(props.titleKey)}</div>
      <div className="mt-2 text-sm leading-relaxed text-muted-foreground">{t(props.textKey)}</div>
    </div>
  )
}
```

- [ ] **Step 2: 接入 OnboardingApp**

把调试用的 placeholder 替换为：

```tsx
{step === 'intro' && (
  <OnboardingStepIntro onStart={() => setStep('select')} />
)}
{step === 'select' && (
  <div className="text-sm text-muted-foreground">select (next task)</div>
)}
{step === 'config' && (
  <div className="text-sm text-muted-foreground">config (next task)</div>
)}
{step === 'runtime' && (
  <div className="text-sm text-muted-foreground">runtime (next task)</div>
)}
```

import 块新增：

```tsx
import { OnboardingStepIntro } from './OnboardingStepIntro'
```

- [ ] **Step 3: 启动验证**

```bash
task dev
```

期望：进入 onboarding 窗口默认是 Step 1：大标题 + 3 张介绍卡 + 「退出」「开始配置」按钮。点击"开始配置"切换到 select 占位。点击「退出」整个应用退出。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/onboarding/
git commit -m "feat(onboarding): implement intro step"
```

---

## Task 12: OnboardingStepSelect

**Files:**
- Create: `frontend/src/components/onboarding/OnboardingStepSelect.tsx`
- Modify: `frontend/src/components/onboarding/OnboardingApp.tsx`

- [ ] **Step 1: 创建 OnboardingStepSelect.tsx**

```tsx
import { useEffect, useState } from 'react'
import { useTranslation } from 'react-i18next'
import { Config } from '@bindings/gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/config'
import { cn } from '@/lib/utils'
import type { SupportedProvider } from '@/types/settings'

type Props = {
  selected: SupportedProvider | null
  onSelect: (p: SupportedProvider) => void
  onBack: () => void
  onNext: () => void
}

export function OnboardingStepSelect(props: Props) {
  const { t } = useTranslation()
  const [providers, setProviders] = useState<SupportedProvider[]>([])
  const [loadFailed, setLoadFailed] = useState<boolean>(false)

  useEffect(() => {
    let cancelled = false
    Config.SupportedProviderList({})
      .then((result) => {
        if (cancelled) return
        if (result?.supported_providers) {
          setProviders(
            result.supported_providers.map((p) => ({
              type: p.type,
              icon: p.icon,
              name: p.name,
              description: p.description,
              base_url: p.base_url,
            })),
          )
        }
      })
      .catch(() => {
        if (!cancelled) setLoadFailed(true)
      })
    return () => {
      cancelled = true
    }
  }, [])

  return (
    <div className="flex h-full min-h-0 flex-col gap-4">
      <div className="space-y-1">
        <h2 className="text-lg font-semibold">{t('onboarding.steps.select')}</h2>
        <p className="text-xs text-muted-foreground">
          {loadFailed ? t('onboarding.select.loadFailed') : t('onboarding.select.hint')}
        </p>
      </div>

      <div className="min-h-0 flex-1 overflow-y-auto pr-1">
        {providers.length === 0 && !loadFailed && (
          <div className="py-8 text-center text-xs text-muted-foreground">…</div>
        )}
        {providers.length === 0 && loadFailed && (
          <div className="py-8 text-center text-xs text-muted-foreground">
            {t('onboarding.select.empty')}
          </div>
        )}
        <div className="space-y-2">
          {providers.map((provider) => (
            <button
              key={provider.type}
              type="button"
              onClick={() => props.onSelect(provider)}
              className={cn(
                'flex w-full items-start gap-4 rounded-2xl border p-4 text-left transition-colors',
                props.selected?.type === provider.type
                  ? 'border-primary bg-primary/10'
                  : 'border-border bg-background hover:bg-accent',
              )}
            >
              {provider.icon && (
                <img
                  src={provider.icon}
                  alt={provider.name}
                  className="h-10 w-10 rounded-xl object-contain"
                  onError={(e) => {
                    ;(e.currentTarget as HTMLImageElement).style.display = 'none'
                  }}
                />
              )}
              <div className="min-w-0 flex-1">
                <div className="text-sm font-semibold text-foreground">{provider.name}</div>
                <div className="mt-0.5 text-xs text-muted-foreground">{provider.description}</div>
                {provider.base_url && (
                  <div className="mt-1 text-xs text-muted-foreground/70">{provider.base_url}</div>
                )}
              </div>
            </button>
          ))}
        </div>
      </div>

      <div className="flex shrink-0 items-center justify-between border-t border-border pt-4">
        <button
          type="button"
          onClick={props.onBack}
          className="rounded-xl border border-border px-5 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent"
        >
          {t('onboarding.actions.back')}
        </button>
        <button
          type="button"
          disabled={!props.selected}
          onClick={props.onNext}
          className="rounded-xl bg-primary px-5 py-2 text-sm font-medium text-primary-foreground transition-opacity disabled:opacity-40"
        >
          {t('onboarding.actions.next')}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 接入 OnboardingApp**

state 新增：

```tsx
const [provider, setProvider] = useState<SupportedProvider | null>(null)
```

import：

```tsx
import { OnboardingStepSelect } from './OnboardingStepSelect'
import type { SupportedProvider } from '@/types/settings'
```

替换 select 占位：

```tsx
{step === 'select' && (
  <OnboardingStepSelect
    selected={provider}
    onSelect={setProvider}
    onBack={() => setStep('intro')}
    onNext={() => setStep('config')}
  />
)}
```

- [ ] **Step 3: 启动验证**

```bash
task dev
```

期望：进入 select 步显示供应商列表，可选中、点击「下一步」切到 config 占位、「返回」切回 intro。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/onboarding/
git commit -m "feat(onboarding): implement select provider step"
```

---

## Task 13: 重构 AddProviderStepForm 引入 mode prop

**Files:**
- Modify: `frontend/src/components/settings/providers/AddProviderStepForm.tsx`

**说明：** 让 `AddProviderStepForm` 在 `mode === 'onboarding'` 时提交到 `Onboarding.SaveProviderAndMarkInitialized`，其余字段保持一致。不改变现有 `AddProviderApp` 的行为。

- [ ] **Step 1: 修改 props 与 submit 逻辑**

在文件顶部 import 块追加：

```tsx
import { Onboarding } from '@bindings/gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding'
```

更新组件签名：

```tsx
export function AddProviderStepForm(props: {
  provider: SupportedProvider
  onDone: () => void
  mode?: 'settings' | 'onboarding'
}) {
```

`handleSubmit` 中替换提交逻辑：

```tsx
      const input = new CreateProviderInput({
        provider_name: name,
        provider_type: props.provider.type as ProviderType,
        base_url: baseUrl,
        api_key: apiKey,
        enable,
        default_model: defaultModel || null,
        models: allModels,
      })

      if (props.mode === 'onboarding') {
        await Onboarding.SaveProviderAndMarkInitialized({
          provider_name: input.provider_name,
          provider_type: input.provider_type,
          base_url: input.base_url,
          api_key: input.api_key,
          enable: input.enable,
          default_model: input.default_model,
          models: input.models,
        })
      } else {
        await ProviderBinding.CreateProvider(input)
      }
      props.onDone()
```

> 实际入参字段名以 `bindings/.../onboarding_dto/models.ts` 自动生成的 TypeScript 定义为准；如生成的字段使用 camelCase 或不同名称，按生成结果对齐。

- [ ] **Step 2: 验证原有 AddProvider 流程未坏**

```bash
task dev
```

打开应用 → 设置 → 添加供应商 → 第二步表单 → 提交。期望：与之前一致。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/components/settings/providers/AddProviderStepForm.tsx
git commit -m "refactor(provider): allow AddProviderStepForm to target onboarding service"
```

---

## Task 14: OnboardingStepForm

**Files:**
- Create: `frontend/src/components/onboarding/OnboardingStepForm.tsx`
- Modify: `frontend/src/components/onboarding/OnboardingApp.tsx`

- [ ] **Step 1: 创建 OnboardingStepForm.tsx**

```tsx
import { useTranslation } from 'react-i18next'
import { AddProviderStepForm } from '@/components/settings/providers/AddProviderStepForm'
import type { SupportedProvider } from '@/types/settings'

type Props = {
  provider: SupportedProvider
  onBack: () => void
  onDone: () => void
}

export function OnboardingStepForm(props: Props) {
  const { t } = useTranslation()

  return (
    <div className="flex h-full min-h-0 flex-col gap-4">
      <div className="space-y-1">
        <h2 className="text-lg font-semibold">{t('onboarding.config.title', { name: props.provider.name })}</h2>
        <p className="text-xs text-muted-foreground">{props.provider.description || t('onboarding.config.hintFallback')}</p>
      </div>

      <div className="min-h-0 flex-1 overflow-hidden">
        <AddProviderStepForm
          provider={props.provider}
          mode="onboarding"
          onDone={props.onDone}
        />
      </div>

      <div className="flex shrink-0 items-center justify-between border-t border-border pt-4">
        <button
          type="button"
          onClick={props.onBack}
          className="rounded-xl border border-border px-5 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent"
        >
          {t('onboarding.actions.back')}
        </button>
        <button
          type="submit"
          form="add-provider-form"
          className="rounded-xl bg-primary px-5 py-2 text-sm font-medium text-primary-foreground"
        >
          {t('onboarding.actions.next')}
        </button>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: 接入 OnboardingApp**

import：

```tsx
import { OnboardingStepForm } from './OnboardingStepForm'
```

替换 config 占位：

```tsx
{step === 'config' && provider && (
  <OnboardingStepForm
    provider={provider}
    onBack={() => setStep('select')}
    onDone={() => setStep('runtime')}
  />
)}
```

- [ ] **Step 3: 启动验证（端到端）**

```bash
rm -f ~/.lemontea/init.json
task dev
```

走完 intro → select → config → 提交。期望：
- 提交成功后切到 runtime 占位
- 退出后重启应用，由于 init.json 已存在，直接进入主窗口

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/onboarding/
git commit -m "feat(onboarding): implement provider config step"
```

---

## Task 15: 创建 runtime 服务骨架 + GetStatus + state.json

**Files:**
- Create: `backend/service/runtime/runtime_constants.go`
- Create: `backend/service/runtime/runtime.go`
- Create: `backend/service/runtime/runtime_implement.go`
- Create: `backend/service/runtime/runtime_internal.go`
- Create: `backend/service/runtime/runtime_dto/get_status.go`
- Create: `backend/service/runtime/runtime_test.go`
- Modify: `backend/pkg/ierror/ierrors.go`
- Modify: `backend/pkg/i18n/resources_en_zh_cn.go`
- Modify: `backend/pkg/i18n/resources_en_us.go`
- Modify: `main.go`

- [ ] **Step 1: 错误码与文案**

`backend/pkg/ierror/ierrors.go` 追加：

```go
	// Runtime error codes
	ErrRuntimeUnsupportedOS    errorCode = "ierror.runtime.unsupported_os"
	ErrRuntimeFetchSums        errorCode = "ierror.runtime.fetch_sums"
	ErrRuntimeChecksumMismatch errorCode = "ierror.runtime.checksum_mismatch"
	ErrRuntimeDownload         errorCode = "ierror.runtime.download"
	ErrRuntimeExtract          errorCode = "ierror.runtime.extract"
	ErrRuntimeWriteState       errorCode = "ierror.runtime.write_state"
	ErrRuntimeReadState        errorCode = "ierror.runtime.read_state"
```

`resources_en_zh_cn.go` 追加：

```go
	"ierror.runtime.unsupported_os":     "不支持的操作系统或架构",
	"ierror.runtime.fetch_sums":         "获取校验文件失败",
	"ierror.runtime.checksum_mismatch":  "运行时安装包校验失败",
	"ierror.runtime.download":           "下载运行时失败",
	"ierror.runtime.extract":            "解压运行时失败",
	"ierror.runtime.write_state":        "写入运行时状态失败",
	"ierror.runtime.read_state":         "读取运行时状态失败",
```

`resources_en_us.go` 追加：

```go
	"ierror.runtime.unsupported_os":     "Unsupported operating system or architecture",
	"ierror.runtime.fetch_sums":         "Failed to fetch checksums",
	"ierror.runtime.checksum_mismatch":  "Runtime archive checksum mismatch",
	"ierror.runtime.download":           "Failed to download runtime",
	"ierror.runtime.extract":            "Failed to extract runtime",
	"ierror.runtime.write_state":        "Failed to write runtime state",
	"ierror.runtime.read_state":         "Failed to read runtime state",
```

- [ ] **Step 2: 创建 runtime_constants.go**

```go
package runtime

// NodeLTSVersion is the Node.js LTS version pinned by the application.
// Update this constant when bumping the embedded Node runtime.
const NodeLTSVersion = "v22.11.0"

// NodeDistBaseURL is the base URL for official Node.js distribution downloads.
const NodeDistBaseURL = "https://nodejs.org/dist"

// RuntimeStateFileName is the file used to persist runtime download state.
const RuntimeStateFileName = "state.json"

// NodeSubdir is the directory under data dir that holds the Node runtime tree.
const NodeSubdir = "runtime/node"
```

- [ ] **Step 3: 创建 runtime_internal.go**

```go
package runtime

import (
	"encoding/json"
	"fmt"
	"os"
	"path/filepath"
	"runtime"
	"time"

	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/dir"
)

// RuntimeState represents the persisted Node runtime install state.
type RuntimeState struct {
	State       string    `json:"state"`
	Version     string    `json:"version"`
	InstallDir  string    `json:"install_dir"`
	NodePath    string    `json:"node_path"`
	NpmPath     string    `json:"npm_path"`
	ErrorMsg    string    `json:"error_msg"`
	InstalledAt time.Time `json:"installed_at"`
	UpdatedAt   time.Time `json:"updated_at"`
}

const (
	StateMissing       = "missing"
	StateDownloading   = "downloading"
	StateReady         = "ready"
	StateFailed        = "failed"
	StatePendingLater  = "pending_later"
)

// runtimeBaseDir returns the absolute directory holding Node runtime artifacts.
func runtimeBaseDir() (string, error) {
	dataDir, err := dir.GetDataDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(dataDir, NodeSubdir), nil
}

// runtimeStatePath returns the absolute path to the state.json file.
func runtimeStatePath() (string, error) {
	base, err := runtimeBaseDir()
	if err != nil {
		return "", err
	}
	return filepath.Join(base, RuntimeStateFileName), nil
}

// loadState reads state.json; missing file yields a fresh {State: missing}.
func loadState() (RuntimeState, error) {
	path, err := runtimeStatePath()
	if err != nil {
		return RuntimeState{}, err
	}
	bytes, err := os.ReadFile(path)
	if err != nil {
		if os.IsNotExist(err) {
			return RuntimeState{State: StateMissing}, nil
		}
		return RuntimeState{}, err
	}
	var s RuntimeState
	if err := json.Unmarshal(bytes, &s); err != nil {
		return RuntimeState{}, err
	}
	return s, nil
}

// saveState writes the runtime state to state.json, creating the directory tree as needed.
func saveState(s RuntimeState) error {
	base, err := runtimeBaseDir()
	if err != nil {
		return err
	}
	if err := os.MkdirAll(base, 0o755); err != nil {
		return err
	}
	s.UpdatedAt = time.Now()
	bytes, err := json.MarshalIndent(s, "", "  ")
	if err != nil {
		return err
	}
	path, err := runtimeStatePath()
	if err != nil {
		return err
	}
	return os.WriteFile(path, bytes, 0o644)
}

// platformSlug returns the Node.js dist OS slug for the current platform (darwin/linux/win).
func platformSlug() (string, error) {
	switch runtime.GOOS {
	case "darwin":
		return "darwin", nil
	case "linux":
		return "linux", nil
	case "windows":
		return "win", nil
	default:
		return "", fmt.Errorf("unsupported os: %s", runtime.GOOS)
	}
}

// archSlug returns the Node.js dist arch slug (x64 / arm64).
func archSlug() (string, error) {
	switch runtime.GOARCH {
	case "amd64":
		return "x64", nil
	case "arm64":
		return "arm64", nil
	default:
		return "", fmt.Errorf("unsupported arch: %s", runtime.GOARCH)
	}
}

// archiveExt returns the archive extension for the current OS (tar.gz / zip).
func archiveExt() (string, error) {
	switch runtime.GOOS {
	case "darwin", "linux":
		return "tar.gz", nil
	case "windows":
		return "zip", nil
	default:
		return "", fmt.Errorf("unsupported os: %s", runtime.GOOS)
	}
}

// archiveURL returns the full download URL for the configured Node version on current platform.
func archiveURL(version string) (string, error) {
	os1, err := platformSlug()
	if err != nil {
		return "", err
	}
	arch, err := archSlug()
	if err != nil {
		return "", err
	}
	ext, err := archiveExt()
	if err != nil {
		return "", err
	}
	return fmt.Sprintf("%s/%s/node-%s-%s-%s.%s", NodeDistBaseURL, version, version, os1, arch, ext), nil
}

// sha256SumsURL returns the SHA256SUMS.txt URL for the configured Node version.
func sha256SumsURL(version string) string {
	return fmt.Sprintf("%s/%s/SHASUMS256.txt", NodeDistBaseURL, version)
}
```

- [ ] **Step 4: 创建 runtime_implement.go**

```go
package runtime

import (
	"context"

	"github.com/wailsapp/wails/v3/pkg/application"
)

// ServiceStartup captures the Wails application handle once the service is registered.
func (s *Runtime) ServiceStartup(ctx context.Context, options application.ServiceOptions) error {
	s.wailsApp = application.Get()
	return nil
}
```

- [ ] **Step 5: 创建 runtime.go 与 GetStatus**

```go
package runtime

import (
	"context"
	"sync"

	"github.com/wailsapp/wails/v3/pkg/application"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/ierror"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/runtime/runtime_dto"
)

// Runtime manages local Node.js runtime download and lifecycle.
type Runtime struct {
	wailsApp *application.App

	mu         sync.Mutex
	cancelFunc context.CancelFunc
}

// NewRuntime constructs the Runtime service.
func NewRuntime() *Runtime {
	return &Runtime{}
}

// GetStatus returns the current persisted Node runtime state.
func (s *Runtime) GetStatus(ctx context.Context, input runtime_dto.GetStatusInput) (*runtime_dto.GetStatusOutput, error) {
	state, err := loadState()
	if err != nil {
		return nil, ierror.Error(ierror.ErrRuntimeReadState, err)
	}
	return &runtime_dto.GetStatusOutput{
		State:      state.State,
		Version:    state.Version,
		InstallDir: state.InstallDir,
		NodePath:   state.NodePath,
		NpmPath:    state.NpmPath,
		ErrorMsg:   state.ErrorMsg,
	}, nil
}
```

- [ ] **Step 6: 创建 runtime_dto/get_status.go**

```go
package runtime_dto

type GetStatusInput struct{}

type GetStatusOutput struct {
	State      string `json:"state"`
	Version    string `json:"version"`
	InstallDir string `json:"install_dir"`
	NodePath   string `json:"node_path"`
	NpmPath    string `json:"npm_path"`
	ErrorMsg   string `json:"error_msg"`
}
```

- [ ] **Step 7: 写单测**

`backend/service/runtime/runtime_test.go`：

```go
package runtime

import (
	"strings"
	"testing"
)

func withTempDataDir(t *testing.T) string {
	t.Helper()
	tmp := t.TempDir()
	t.Setenv("LEMONTEA_DATA_DIR", tmp)
	return tmp
}

func TestArchiveURL_PlatformShape(t *testing.T) {
	url, err := archiveURL(NodeLTSVersion)
	if err != nil {
		t.Fatalf("unexpected: %v", err)
	}
	if !strings.HasPrefix(url, NodeDistBaseURL+"/"+NodeLTSVersion+"/node-"+NodeLTSVersion+"-") {
		t.Fatalf("unexpected url prefix: %s", url)
	}
}

func TestLoadState_MissingFile(t *testing.T) {
	withTempDataDir(t)
	s, err := loadState()
	if err != nil {
		t.Fatalf("unexpected: %v", err)
	}
	if s.State != StateMissing {
		t.Fatalf("expected missing, got %s", s.State)
	}
}

func TestSaveAndLoadState(t *testing.T) {
	withTempDataDir(t)
	if err := saveState(RuntimeState{State: StateReady, Version: NodeLTSVersion}); err != nil {
		t.Fatalf("save: %v", err)
	}
	s, err := loadState()
	if err != nil {
		t.Fatalf("load: %v", err)
	}
	if s.State != StateReady || s.Version != NodeLTSVersion {
		t.Fatalf("unexpected state: %+v", s)
	}
}
```

- [ ] **Step 8: 跑测试**

```bash
go test ./backend/service/runtime/...
```

期望：3 个测试通过。如果当前主机 OS/arch 不在 darwin/linux/windows × amd64/arm64 集合内，`TestArchiveURL_PlatformShape` 会失败；本项目开发环境为 darwin/arm64 + linux/amd64，应可通过。

- [ ] **Step 9: 在 main.go 注册 runtime 服务**

import 块追加：

```go
	runtimeSvc "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/runtime"
```

`Services: []application.Service{...}` 中追加：

```go
		application.NewService(runtimeSvc.NewRuntime()),
```

- [ ] **Step 10: 启动验证**

```bash
task dev
```

期望：应用启动正常。前端绑定已生成 `Runtime.GetStatus`。

- [ ] **Step 11: Commit**

```bash
git add backend/service/runtime/ backend/pkg/ierror/ backend/pkg/i18n/ main.go
git commit -m "feat(runtime): add Node runtime service skeleton with GetStatus and state persistence"
```

---

## Task 16: DownloadNode 实现（含进度事件）

**Files:**
- Create: `backend/service/runtime/runtime_dto/download_node.go`
- Modify: `backend/service/runtime/runtime.go`
- Modify: `backend/service/runtime/runtime_internal.go`

- [ ] **Step 1: 创建 DTO**

`runtime_dto/download_node.go`：

```go
package runtime_dto

type DownloadNodeInput struct{}
type DownloadNodeOutput struct{}
```

- [ ] **Step 2: runtime_internal.go 追加下载/解压/校验函数**

`runtime_internal.go` 文件顶部 import 块追加：

```go
"archive/tar"
"archive/zip"
"bufio"
"compress/gzip"
"context"
"crypto/sha256"
"encoding/hex"
"io"
"net/http"
"strings"
```

文件末尾追加：

```go
// fetchSha256 downloads SHASUMS256.txt and returns the sha256 for the file name we want.
func fetchSha256(ctx context.Context, sumsURL, archiveName string) (string, error) {
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, sumsURL, nil)
	if err != nil {
		return "", err
	}
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()
	if resp.StatusCode != http.StatusOK {
		return "", fmt.Errorf("status %d", resp.StatusCode)
	}
	scanner := bufio.NewScanner(resp.Body)
	for scanner.Scan() {
		line := scanner.Text()
		parts := strings.Fields(line)
		if len(parts) == 2 && parts[1] == archiveName {
			return parts[0], nil
		}
	}
	if err := scanner.Err(); err != nil {
		return "", err
	}
	return "", fmt.Errorf("checksum not found for %s", archiveName)
}

// downloadArchive streams the archive to dest, emitting progress via report.
// Returns the SHA256 hex of the downloaded bytes for verification.
func downloadArchive(ctx context.Context, url, dest string, report func(received, total int64)) (string, error) {
	req, err := http.NewRequestWithContext(ctx, http.MethodGet, url, nil)
	if err != nil {
		return "", err
	}
	resp, err := http.DefaultClient.Do(req)
	if err != nil {
		return "", err
	}
	defer resp.Body.Close()
	if resp.StatusCode != http.StatusOK {
		return "", fmt.Errorf("status %d", resp.StatusCode)
	}

	if err := os.MkdirAll(filepath.Dir(dest), 0o755); err != nil {
		return "", err
	}
	out, err := os.Create(dest)
	if err != nil {
		return "", err
	}
	defer out.Close()

	hasher := sha256.New()
	total := resp.ContentLength
	var received int64
	buf := make([]byte, 64*1024)
	for {
		if err := ctx.Err(); err != nil {
			return "", err
		}
		n, readErr := resp.Body.Read(buf)
		if n > 0 {
			if _, err := out.Write(buf[:n]); err != nil {
				return "", err
			}
			hasher.Write(buf[:n])
			received += int64(n)
			if report != nil {
				report(received, total)
			}
		}
		if readErr == io.EOF {
			break
		}
		if readErr != nil {
			return "", readErr
		}
	}

	return hex.EncodeToString(hasher.Sum(nil)), nil
}

// extractTarGz extracts the tar.gz archive at src to destDir. Returns the top-level extracted folder name.
func extractTarGz(ctx context.Context, src, destDir string, report func(received, total int64)) (string, error) {
	f, err := os.Open(src)
	if err != nil {
		return "", err
	}
	defer f.Close()
	gz, err := gzip.NewReader(f)
	if err != nil {
		return "", err
	}
	defer gz.Close()
	tr := tar.NewReader(gz)

	var topDir string
	stat, _ := f.Stat()
	total := stat.Size()

	if err := os.MkdirAll(destDir, 0o755); err != nil {
		return "", err
	}

	for {
		if err := ctx.Err(); err != nil {
			return "", err
		}
		header, err := tr.Next()
		if err == io.EOF {
			break
		}
		if err != nil {
			return "", err
		}
		if topDir == "" {
			topDir = strings.SplitN(header.Name, "/", 2)[0]
		}
		target := filepath.Join(destDir, header.Name)
		switch header.Typeflag {
		case tar.TypeDir:
			if err := os.MkdirAll(target, os.FileMode(header.Mode)); err != nil {
				return "", err
			}
		case tar.TypeReg:
			if err := os.MkdirAll(filepath.Dir(target), 0o755); err != nil {
				return "", err
			}
			out, err := os.OpenFile(target, os.O_CREATE|os.O_WRONLY|os.O_TRUNC, os.FileMode(header.Mode))
			if err != nil {
				return "", err
			}
			if _, err := io.Copy(out, tr); err != nil {
				out.Close()
				return "", err
			}
			out.Close()
		case tar.TypeSymlink:
			_ = os.Symlink(header.Linkname, target)
		}
		if report != nil {
			pos, _ := f.Seek(0, io.SeekCurrent)
			report(pos, total)
		}
	}
	return topDir, nil
}

// extractZip extracts a zip archive (Windows). Returns the top-level directory name.
func extractZip(ctx context.Context, src, destDir string, report func(received, total int64)) (string, error) {
	r, err := zip.OpenReader(src)
	if err != nil {
		return "", err
	}
	defer r.Close()

	var topDir string
	total := int64(len(r.File))
	if err := os.MkdirAll(destDir, 0o755); err != nil {
		return "", err
	}
	for i, file := range r.File {
		if err := ctx.Err(); err != nil {
			return "", err
		}
		if topDir == "" {
			topDir = strings.SplitN(file.Name, "/", 2)[0]
		}
		target := filepath.Join(destDir, file.Name)
		if file.FileInfo().IsDir() {
			if err := os.MkdirAll(target, 0o755); err != nil {
				return "", err
			}
			continue
		}
		if err := os.MkdirAll(filepath.Dir(target), 0o755); err != nil {
			return "", err
		}
		rc, err := file.Open()
		if err != nil {
			return "", err
		}
		out, err := os.OpenFile(target, os.O_CREATE|os.O_WRONLY|os.O_TRUNC, file.Mode())
		if err != nil {
			rc.Close()
			return "", err
		}
		if _, err := io.Copy(out, rc); err != nil {
			rc.Close()
			out.Close()
			return "", err
		}
		rc.Close()
		out.Close()
		if report != nil {
			report(int64(i+1), total)
		}
	}
	return topDir, nil
}
```

> 注：因 `context.Context` 现在被使用，文件顶部 import 也要追加 `"context"`。

- [ ] **Step 3: runtime.go 添加 DownloadNode 与进度事件**

`runtime.go` 顶部 import 块在原有基础上追加：

```go
"fmt"
"os"
"path/filepath"
"runtime"
"strings"
"time"
```

`Runtime` 结构体重写为（在原有 mu/cancelFunc 基础上加 running）：

```go
type Runtime struct {
	wailsApp *application.App

	mu         sync.Mutex
	cancelFunc context.CancelFunc
	running    bool
}
```

文件末尾追加：

```go
// progressPayload is sent on the "runtime.node.progress" event.
type progressPayload struct {
	Phase    string `json:"phase"`
	Received int64  `json:"received"`
	Total    int64  `json:"total"`
	Percent  int    `json:"percent"`
}

// emitProgress publishes a progress update for the frontend.
func (s *Runtime) emitProgress(phase string, received, total int64) {
	percent := 0
	if total > 0 {
		percent = int(received * 100 / total)
	}
	if s.wailsApp != nil {
		s.wailsApp.Event.Emit("runtime.node.progress", progressPayload{
			Phase:    phase,
			Received: received,
			Total:    total,
			Percent:  percent,
		})
	}
}

// failState records a failure into state.json and emits a terminal progress event.
func (s *Runtime) failState(msg string) {
	_ = saveState(RuntimeState{
		State:    StateFailed,
		Version:  NodeLTSVersion,
		ErrorMsg: msg,
	})
	s.emitProgress("verify", 0, 0)
}

// DownloadNode starts an asynchronous Node.js download, returning immediately;
// progress is published via the "runtime.node.progress" event.
func (s *Runtime) DownloadNode(ctx context.Context, input runtime_dto.DownloadNodeInput) (*runtime_dto.DownloadNodeOutput, error) {
	s.mu.Lock()
	if s.running {
		s.mu.Unlock()
		return &runtime_dto.DownloadNodeOutput{}, nil
	}
	runCtx, cancel := context.WithCancel(context.Background())
	s.cancelFunc = cancel
	s.running = true
	s.mu.Unlock()

	go s.runDownload(runCtx)

	return &runtime_dto.DownloadNodeOutput{}, nil
}

// runDownload performs the actual download/extract/verify pipeline.
func (s *Runtime) runDownload(ctx context.Context) {
	defer func() {
		s.mu.Lock()
		s.running = false
		s.cancelFunc = nil
		s.mu.Unlock()
	}()

	_ = saveState(RuntimeState{State: StateDownloading, Version: NodeLTSVersion})

	url, err := archiveURL(NodeLTSVersion)
	if err != nil {
		s.failState(err.Error())
		return
	}
	archiveName := filepath.Base(url)
	sumsURL := sha256SumsURL(NodeLTSVersion)

	expectedSum, err := fetchSha256(ctx, sumsURL, archiveName)
	if err != nil {
		s.failState(err.Error())
		return
	}

	base, err := runtimeBaseDir()
	if err != nil {
		s.failState(err.Error())
		return
	}
	archivePath := filepath.Join(base, archiveName)

	actualSum, err := downloadArchive(ctx, url, archivePath, func(received, total int64) {
		s.emitProgress("download", received, total)
	})
	if err != nil {
		s.failState(err.Error())
		return
	}
	if !strings.EqualFold(actualSum, expectedSum) {
		s.failState(fmt.Sprintf("checksum mismatch: expected %s got %s", expectedSum, actualSum))
		return
	}

	installRoot := filepath.Join(base, NodeLTSVersion)
	_ = os.RemoveAll(installRoot)
	if err := os.MkdirAll(installRoot, 0o755); err != nil {
		s.failState(err.Error())
		return
	}

	var topDir string
	ext, _ := archiveExt()
	switch ext {
	case "tar.gz":
		topDir, err = extractTarGz(ctx, archivePath, installRoot, func(received, total int64) {
			s.emitProgress("extract", received, total)
		})
	case "zip":
		topDir, err = extractZip(ctx, archivePath, installRoot, func(received, total int64) {
			s.emitProgress("extract", received, total)
		})
	default:
		err = fmt.Errorf("unsupported ext: %s", ext)
	}
	if err != nil {
		s.failState(err.Error())
		return
	}

	s.emitProgress("verify", 1, 1)

	extractedRoot := filepath.Join(installRoot, topDir)
	var nodePath, npmPath string
	if runtime.GOOS == "windows" {
		nodePath = filepath.Join(extractedRoot, "node.exe")
		npmPath = filepath.Join(extractedRoot, "npm.cmd")
	} else {
		nodePath = filepath.Join(extractedRoot, "bin", "node")
		npmPath = filepath.Join(extractedRoot, "bin", "npm")
	}

	_ = os.Remove(archivePath)

	if err := saveState(RuntimeState{
		State:       StateReady,
		Version:     NodeLTSVersion,
		InstallDir:  extractedRoot,
		NodePath:    nodePath,
		NpmPath:     npmPath,
		InstalledAt: time.Now(),
	}); err != nil {
		s.failState(err.Error())
		return
	}
}
```

- [ ] **Step 4: 编译通过**

```bash
go build ./...
```

期望：无错误。如有 import 重复或缺失，按编译提示修复。

- [ ] **Step 5: Commit**

```bash
git add backend/service/runtime/
git commit -m "feat(runtime): implement asynchronous Node download with sha256 verification and progress events"
```

---

## Task 17: CancelDownload + MarkDownloadLater

**Files:**
- Create: `backend/service/runtime/runtime_dto/cancel_download.go`
- Create: `backend/service/runtime/runtime_dto/mark_download_later.go`
- Modify: `backend/service/runtime/runtime.go`

- [ ] **Step 1: 创建 DTO**

`runtime_dto/cancel_download.go`：

```go
package runtime_dto

type CancelDownloadInput struct{}
type CancelDownloadOutput struct{}
```

`runtime_dto/mark_download_later.go`：

```go
package runtime_dto

type MarkDownloadLaterInput struct{}
type MarkDownloadLaterOutput struct{}
```

- [ ] **Step 2: 在 runtime.go 末尾追加方法**

```go
// CancelDownload aborts an in-progress download (if any).
func (s *Runtime) CancelDownload(ctx context.Context, input runtime_dto.CancelDownloadInput) (*runtime_dto.CancelDownloadOutput, error) {
	s.mu.Lock()
	cancel := s.cancelFunc
	s.mu.Unlock()
	if cancel != nil {
		cancel()
	}
	return &runtime_dto.CancelDownloadOutput{}, nil
}

// MarkDownloadLater records a user choice to download the runtime later.
func (s *Runtime) MarkDownloadLater(ctx context.Context, input runtime_dto.MarkDownloadLaterInput) (*runtime_dto.MarkDownloadLaterOutput, error) {
	current, err := loadState()
	if err != nil {
		return nil, ierror.Error(ierror.ErrRuntimeReadState, err)
	}
	current.State = StatePendingLater
	if err := saveState(current); err != nil {
		return nil, ierror.Error(ierror.ErrRuntimeWriteState, err)
	}
	return &runtime_dto.MarkDownloadLaterOutput{}, nil
}
```

- [ ] **Step 3: 编译通过**

```bash
go build ./...
```

- [ ] **Step 4: Commit**

```bash
git add backend/service/runtime/
git commit -m "feat(runtime): add CancelDownload and MarkDownloadLater"
```

---

## Task 18: OnboardingStepRuntime（前端）

**Files:**
- Create: `frontend/src/components/onboarding/OnboardingStepRuntime.tsx`
- Modify: `frontend/src/components/onboarding/OnboardingApp.tsx`

- [ ] **Step 1: 创建 OnboardingStepRuntime.tsx**

```tsx
import { useEffect, useState } from 'react'
import { useTranslation } from 'react-i18next'
import { Events } from '@wailsio/runtime'
import { Runtime } from '@bindings/gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/runtime'
import { Onboarding } from '@bindings/gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/onboarding'
import { cn } from '@/lib/utils'

type Phase = 'download' | 'extract' | 'verify'

type RuntimeState =
  | { kind: 'idle' }
  | { kind: 'downloading'; phase: Phase; received: number; total: number; percent: number }
  | { kind: 'ready'; version: string; installDir: string }
  | { kind: 'failed'; message: string }

type ProgressEventData = {
  phase: Phase
  received: number
  total: number
  percent: number
}

export function OnboardingStepRuntime() {
  const { t } = useTranslation()
  const [state, setState] = useState<RuntimeState>({ kind: 'idle' })

  useEffect(() => {
    let cancelled = false
    Runtime.GetStatus({})
      .then((res) => {
        if (cancelled || !res) return
        if (res.state === 'ready') {
          setState({ kind: 'ready', version: res.version, installDir: res.install_dir })
        } else if (res.state === 'downloading') {
          setState({ kind: 'downloading', phase: 'download', received: 0, total: 0, percent: 0 })
        } else if (res.state === 'failed') {
          setState({ kind: 'failed', message: res.error_msg || '' })
        }
      })
      .catch(() => undefined)

    const off = Events.On('runtime.node.progress', (evt: { data: ProgressEventData }) => {
      const p = evt.data
      setState((prev) => {
        if (prev.kind === 'ready' || prev.kind === 'failed') return prev
        return { kind: 'downloading', phase: p.phase, received: p.received, total: p.total, percent: p.percent }
      })
      if (p.phase === 'verify' && p.percent === 100) {
        Runtime.GetStatus({})
          .then((res) => {
            if (!res) return
            if (res.state === 'ready') {
              setState({ kind: 'ready', version: res.version, installDir: res.install_dir })
            } else if (res.state === 'failed') {
              setState({ kind: 'failed', message: res.error_msg || '' })
            }
          })
          .catch(() => undefined)
      }
    })

    return () => {
      cancelled = true
      off()
    }
  }, [])

  const startDownload = () => {
    setState({ kind: 'downloading', phase: 'download', received: 0, total: 0, percent: 0 })
    void Runtime.DownloadNode({})
  }

  const downloadLater = async () => {
    await Runtime.MarkDownloadLater({})
    await Onboarding.EnterHome({})
  }

  const cancelDownload = () => {
    void Runtime.CancelDownload({})
    setState({ kind: 'idle' })
  }

  const enterHome = () => {
    void Onboarding.EnterHome({})
  }

  return (
    <div className="flex h-full min-h-0 flex-col gap-4">
      <div className="space-y-1">
        <h2 className="text-lg font-semibold">{t('onboarding.runtime.title')}</h2>
        <p className="text-xs text-muted-foreground">{t('onboarding.runtime.intro')}</p>
      </div>

      <div className="min-h-0 flex-1 overflow-y-auto rounded-2xl border border-border bg-card p-6">
        {state.kind === 'idle' && (
          <div className="flex flex-col items-center justify-center gap-3 py-8 text-center">
            <div className="text-sm text-muted-foreground">{t('onboarding.runtime.stateMissing')}</div>
            <div className="flex gap-2">
              <button
                type="button"
                onClick={downloadLater}
                className="rounded-xl border border-border px-5 py-2 text-sm font-medium text-foreground transition-colors hover:bg-accent"
              >
                {t('onboarding.actions.downloadLater')}
              </button>
              <button
                type="button"
                onClick={startDownload}
                className="rounded-xl bg-primary px-5 py-2 text-sm font-medium text-primary-foreground"
              >
                {t('onboarding.actions.downloadNow')}
              </button>
            </div>
          </div>
        )}

        {state.kind === 'downloading' && (
          <div className="space-y-3">
            <div className="text-sm font-medium">{t(`onboarding.runtime.phase${capitalize(state.phase)}`)}</div>
            <div className="h-2 w-full overflow-hidden rounded-full bg-muted">
              <div
                className={cn('h-full bg-primary transition-all')}
                style={{ width: `${state.percent}%` }}
              />
            </div>
            <div className="text-xs text-muted-foreground">
              {formatBytes(state.received)} / {state.total > 0 ? formatBytes(state.total) : '…'} ({state.percent}%)
            </div>
            <div className="pt-2">
              <button
                type="button"
                onClick={cancelDownload}
                className="rounded-xl border border-border px-4 py-1.5 text-xs font-medium text-foreground transition-colors hover:bg-accent"
              >
                {t('onboarding.actions.cancel')}
              </button>
            </div>
          </div>
        )}

        {state.kind === 'ready' && (
          <div className="flex flex-col items-center justify-center gap-3 py-8 text-center">
            <div className="text-sm font-semibold text-primary">{t('onboarding.runtime.stateReady')}</div>
            <div className="text-xs text-muted-foreground">{state.version}</div>
            <div className="text-xs text-muted-foreground/70">{state.installDir}</div>
          </div>
        )}

        {state.kind === 'failed' && (
          <div className="flex flex-col items-center justify-center gap-3 py-8 text-center">
            <div className="text-sm font-semibold text-destructive">{t('onboarding.runtime.stateFailed')}</div>
            <div className="text-xs text-muted-foreground">{state.message}</div>
            <div className="flex gap-2">
              <button
                type="button"
                onClick={downloadLater}
                className="rounded-xl border border-border px-4 py-1.5 text-xs font-medium text-foreground hover:bg-accent"
              >
                {t('onboarding.actions.skip')}
              </button>
              <button
                type="button"
                onClick={startDownload}
                className="rounded-xl bg-primary px-4 py-1.5 text-xs font-medium text-primary-foreground"
              >
                {t('onboarding.actions.retry')}
              </button>
            </div>
          </div>
        )}
      </div>

      <div className="flex shrink-0 items-center justify-end border-t border-border pt-4">
        <button
          type="button"
          disabled={state.kind === 'downloading'}
          onClick={enterHome}
          className="rounded-xl bg-primary px-5 py-2 text-sm font-medium text-primary-foreground transition-opacity disabled:opacity-40"
        >
          {t('onboarding.actions.enterHome')}
        </button>
      </div>
    </div>
  )
}

function capitalize(s: string): string {
  return s.charAt(0).toUpperCase() + s.slice(1)
}

function formatBytes(n: number): string {
  if (n <= 0) return '0 B'
  const units = ['B', 'KB', 'MB', 'GB']
  let i = 0
  let v = n
  while (v >= 1024 && i < units.length - 1) {
    v /= 1024
    i += 1
  }
  return `${v.toFixed(1)} ${units[i]}`
}
```

> `Events.On` 来自 `@wailsio/runtime`，与项目 `AlertEventProvider.tsx` 一致；`off()` 取消订阅。

- [ ] **Step 2: 接入 OnboardingApp**

import：

```tsx
import { OnboardingStepRuntime } from './OnboardingStepRuntime'
```

替换 runtime 占位：

```tsx
{step === 'runtime' && (
  <OnboardingStepRuntime />
)}
```

- [ ] **Step 3: 端到端验证**

```bash
rm -f ~/.lemontea/init.json
task dev
```

走通完整 4 步：
- intro → select → config（提交）→ runtime
- runtime 选「立即下载」：观察进度条；下载完成后「进入主界面」可用，点击后主窗口出现、onboarding 窗口关闭
- 重启应用：直接进主窗口（init.json 已存在）

`rm -f ~/.lemontea/init.json` 再走一次，runtime 选「稍后下载」，验证立刻进入主窗口。

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/onboarding/
git commit -m "feat(onboarding): implement runtime download step"
```

---

## Task 19: 设置页 NodeRuntimeSection

**Files:**
- Create: `frontend/src/components/settings/general/NodeRuntimeSection.tsx`
- Modify: `frontend/src/components/settings/SettingsShell.tsx`（或当前承载常规 Tab 内容的文件）

> 实施前先确认当前 `frontend/src/components/settings/general/` 是否存在，以及 SettingsApp 的「常规」Tab 是哪个文件渲染。如果常规 Tab 在 `SettingsShell` 之外的组件里，在那个组件里挂载 `NodeRuntimeSection`。

- [ ] **Step 1: 创建 NodeRuntimeSection.tsx**

```tsx
import { useEffect, useState } from 'react'
import { useTranslation } from 'react-i18next'
import { Events } from '@wailsio/runtime'
import { Runtime } from '@bindings/gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/runtime'

type Phase = 'download' | 'extract' | 'verify'

type Snapshot = {
  state: string
  version: string
  install_dir: string
  node_path: string
  npm_path: string
  error_msg: string
  phase?: Phase
  percent?: number
}

export function NodeRuntimeSection() {
  const { t } = useTranslation()
  const [snap, setSnap] = useState<Snapshot | null>(null)

  const refresh = async () => {
    const res = await Runtime.GetStatus({})
    if (res) {
      setSnap({
        state: res.state,
        version: res.version,
        install_dir: res.install_dir,
        node_path: res.node_path,
        npm_path: res.npm_path,
        error_msg: res.error_msg,
      })
    }
  }

  useEffect(() => {
    void refresh()
    const off = Events.On('runtime.node.progress', (evt: { data: { phase: Phase; percent: number } }) => {
      setSnap((prev) =>
        prev ? { ...prev, phase: evt.data.phase, percent: evt.data.percent, state: 'downloading' } : prev,
      )
      if (evt.data.phase === 'verify' && evt.data.percent === 100) {
        void refresh()
      }
    })
    return () => {
      off()
    }
  }, [])

  if (!snap) return null

  const statusKey = {
    missing: 'onboarding.runtime.stateMissing',
    downloading: 'onboarding.runtime.stateDownloading',
    ready: 'onboarding.runtime.stateReady',
    failed: 'onboarding.runtime.stateFailed',
    pending_later: 'onboarding.runtime.statePendingLater',
  }[snap.state] || 'onboarding.runtime.stateMissing'

  const renderActions = () => {
    if (snap.state === 'downloading') {
      return (
        <button
          type="button"
          onClick={() => void Runtime.CancelDownload({})}
          className="rounded-lg border border-border px-3 py-1 text-xs text-foreground hover:bg-accent"
        >
          {t('onboarding.actions.cancel')}
        </button>
      )
    }
    return (
      <button
        type="button"
        onClick={() => void Runtime.DownloadNode({})}
        className="rounded-lg bg-primary px-3 py-1 text-xs text-primary-foreground"
      >
        {t('onboarding.actions.downloadNow')}
      </button>
    )
  }

  return (
    <div className="rounded-xl border border-border bg-card p-4">
      <div className="flex items-center justify-between">
        <div className="space-y-1">
          <div className="text-sm font-semibold">{t('onboarding.runtime.title')}</div>
          <div className="text-xs text-muted-foreground">{t(statusKey)}</div>
          {snap.version && <div className="text-xs text-muted-foreground/70">{snap.version}</div>}
          {snap.install_dir && <div className="text-xs text-muted-foreground/70">{snap.install_dir}</div>}
          {snap.error_msg && <div className="text-xs text-destructive">{snap.error_msg}</div>}
        </div>
        {renderActions()}
      </div>
      {snap.state === 'downloading' && snap.percent !== undefined && (
        <div className="mt-3 h-1.5 overflow-hidden rounded-full bg-muted">
          <div className="h-full bg-primary" style={{ width: `${snap.percent}%` }} />
        </div>
      )}
    </div>
  )
}
```

- [ ] **Step 2: 在「常规」Tab 中渲染**

打开 SettingsShell 或 `frontend/src/components/settings/general/` 的入口 Tab 组件，在合适的位置插入 `<NodeRuntimeSection />`：

```tsx
import { NodeRuntimeSection } from './general/NodeRuntimeSection'

// 在「常规」Tab 的 JSX 内
<NodeRuntimeSection />
```

> 若现有「常规」Tab 用其他文件名（例如 `GeneralTab.tsx`、`SettingsGeneral.tsx`），按实际路径调整。

- [ ] **Step 3: 验证**

```bash
task dev
```

打开 设置 → 常规，期望：
- 看到 Node.js 运行时区块，显示当前状态
- 未装时点「立即下载」开始下载，进度条显示
- 已装时显示已安装版本和路径

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components/settings/
git commit -m "feat(settings): add Node runtime section to general tab"
```

---

## Task 20: 端到端验证清单

**Files:**
- 无新增/修改，纯人工验证。

- [ ] **Step 1: 初次启动流程**

```bash
rm -f ~/.lemontea/init.json
rm -rf ~/.lemontea/runtime
task dev
```

期望：仅 onboarding 窗口打开，主窗口不存在。

- [ ] **Step 2: 四步流程 — 立即下载路径**

走 intro → select → config（提交）→ runtime → 立即下载 → 进入主界面：
- 主窗口打开、onboarding 窗口关闭
- `~/.lemontea/init.json` 已生成
- `~/.lemontea/runtime/node/state.json` 中 `state: ready`、`node_path` 指向解压后的 node 可执行

- [ ] **Step 3: 四步流程 — 稍后下载路径**

```bash
rm -f ~/.lemontea/init.json
rm -rf ~/.lemontea/runtime
task dev
```

走完前 3 步，runtime 选「稍后下载」：
- onboarding 窗口立刻关闭、主窗口打开
- `state.json` 中 `state: pending_later`

- [ ] **Step 4: 已初始化状态下重启**

完全关闭应用后再次启动：直接进入主窗口，不再走 onboarding。

- [ ] **Step 5: 设置页能下载**

打开 设置 → 常规 → Node 运行时区块，从「未安装」或「稍后下载」状态点「立即下载」，可以看到进度条；完成后状态变为「已安装」。

- [ ] **Step 6: 主题 + 字号 + 语言**

在 onboarding 与设置页 NodeRuntimeSection 中：
- 浅色 / 深色模式切换：色彩跟随，无硬编码白底/黑底
- 字号 5 档切换：文字大小变化合理
- 切换 zh-CN / en：文案对应变化

- [ ] **Step 7: 退出**

intro 页点击「退出」：应用整体退出，进程消失。

- [ ] **Step 8: Commit verification log（可选）**

如以上全部通过，可以做一次空 commit 标记里程碑：

```bash
git commit --allow-empty -m "chore(onboarding): verified end-to-end onboarding + runtime flow"
```

---

## 自检备忘

实施过程中若发现以下偏差，应回到对应任务调整：

1. `provider_dto.CreateProviderInput` 字段与 `SaveProviderAndMarkInitializedInput` 不完全对齐 — 以实际字段为准
2. 前端从 bindings 自动生成的 TypeScript 类型字段名（camelCase/snake_case）与计划示例不一致 — 以 bindings 生成为准
3. 「常规」Tab 实际承载文件名不是 `SettingsShell` — 找到承载常规 Tab 内容的组件再挂载 `NodeRuntimeSection`
4. Wails Events 在前端的事件名常量：本计划用字符串 `"runtime.node.progress"`，与后端 `Emit` 字符串一致即可，不需要额外注册
