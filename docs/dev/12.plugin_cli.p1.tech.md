# CLI 插件实施计划 — P1 目录迁移

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把所有扩展目录统一收纳到 `{data_dir}/plugins/` 下，并在应用启动时自动迁移旧布局（`{data_dir}/mcp/`、`{data_dir}/plugin/`）。完成后旧 MCP 工具仍然可用，路径换到新位置。

**Architecture:** 新增 `backend/pkg/migration/` 包负责一次性目录搬迁 + 改写 `config.json` 中扩展路径前缀；新增 `backend/pkg/dir/` 中的路径常量与 helper，所有写扩展子目录的代码统一走 helper；`main.go` 在 storage 初始化之后、服务注册之前调用一次迁移。整套机制靠幂等标记 `{data_dir}/plugins/.migrated_v1` 防重复执行。

**Tech Stack:** Go + Wails v3。

参考需求文档：`docs/dev/12.plugin_cli.md` §2「数据目录布局」。所有实现必须遵循 `docs/dev/00.rules.md`。

---

## 文件结构

### 新建文件

```
backend/
  pkg/
    migration/
      extension_dirs.go
      extension_dirs_test.go
```

### 修改文件

```
backend/
  pkg/
    dir/
      dir.go                 # 新增路径常量 + helper
      dir_test.go            # 新增路径 helper 单测
    mcp/
      manager.go             # ImportMCP / ImportPlugin 用新 helper
      manager_test.go        # 路径断言改成 plugins/mcp/* 与 plugins/plugin/*
main.go                       # 启动时调用 migration.MigrateExtensionDirs
```

---

## Task 1: pkg/dir 新增 plugins 路径常量与 helper

**Files:**
- Modify: `backend/pkg/dir/dir.go`
- Modify: `backend/pkg/dir/dir_test.go`

- [ ] **Step 1: 写失败测试**

把以下测试函数追加到 `backend/pkg/dir/dir_test.go` 末尾：

```go
// TestExtensionPathHelpers 验证 plugins 子目录 helper 返回的拼装路径。
func TestExtensionPathHelpers(t *testing.T) {
	base := filepath.Join("/", "tmp", "lemontea-test")

	cases := []struct {
		name string
		got  string
		want string
	}{
		{"ExtensionsRoot", ExtensionsRoot(base), filepath.Join(base, "plugins")},
		{"MCPRoot", MCPRoot(base), filepath.Join(base, "plugins", "mcp")},
		{"PluginRoot", PluginRoot(base), filepath.Join(base, "plugins", "plugin")},
		{"CLIRoot", CLIRoot(base), filepath.Join(base, "plugins", "cli")},
		{"CLIDataRoot", CLIDataRoot(base), filepath.Join(base, "plugins", "cli_data")},
		{"LegacyMCPRoot", LegacyMCPRoot(base), filepath.Join(base, "mcp")},
		{"LegacyPluginRoot", LegacyPluginRoot(base), filepath.Join(base, "plugin")},
	}

	for _, c := range cases {
		if c.got != c.want {
			t.Fatalf("%s = %q, want %q", c.name, c.got, c.want)
		}
	}
}
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/pkg/dir/ -run TestExtensionPathHelpers -v
```

预期：编译失败（`undefined: ExtensionsRoot` 等）。

- [ ] **Step 3: 在 `backend/pkg/dir/dir.go` 末尾追加 helper 实现**

```go
// PluginsSubdirName is the unified parent directory that holds all extension subdirs under data_dir.
const PluginsSubdirName = "plugins"

// MCPSubdirName is the directory name for MCP bundles under the plugins root.
const MCPSubdirName = "mcp"

// PluginSubdirName is the directory name for Node.js plugins under the plugins root.
const PluginSubdirName = "plugin"

// CLISubdirName is the directory name for CLI plugin installs under the plugins root.
const CLISubdirName = "cli"

// CLIDataSubdirName is the directory name for CLI plugin persistent data under the plugins root.
const CLIDataSubdirName = "cli_data"

// ExtensionsRoot returns the unified extensions parent dir: {dataDir}/plugins.
func ExtensionsRoot(dataDir string) string {
	return filepath.Join(dataDir, PluginsSubdirName)
}

// MCPRoot returns the MCP bundles root: {dataDir}/plugins/mcp.
func MCPRoot(dataDir string) string {
	return filepath.Join(ExtensionsRoot(dataDir), MCPSubdirName)
}

// PluginRoot returns the Node.js plugins root: {dataDir}/plugins/plugin.
func PluginRoot(dataDir string) string {
	return filepath.Join(ExtensionsRoot(dataDir), PluginSubdirName)
}

// CLIRoot returns the CLI plugin install root: {dataDir}/plugins/cli.
func CLIRoot(dataDir string) string {
	return filepath.Join(ExtensionsRoot(dataDir), CLISubdirName)
}

// CLIDataRoot returns the CLI plugin persistent data root: {dataDir}/plugins/cli_data.
func CLIDataRoot(dataDir string) string {
	return filepath.Join(ExtensionsRoot(dataDir), CLIDataSubdirName)
}

// LegacyMCPRoot returns the pre-migration MCP root used by older builds.
func LegacyMCPRoot(dataDir string) string {
	return filepath.Join(dataDir, MCPSubdirName)
}

// LegacyPluginRoot returns the pre-migration Node.js plugin root used by older builds.
func LegacyPluginRoot(dataDir string) string {
	return filepath.Join(dataDir, PluginSubdirName)
}
```

- [ ] **Step 4: 再跑测试，确认通过**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/pkg/dir/ -run TestExtensionPathHelpers -v
```

预期：PASS。

- [ ] **Step 5: 提交**

```bash
git add backend/pkg/dir/
git commit -m "feat(dir): add unified plugins/* path helpers for extensions"
```

---

## Task 2: pkg/mcp/Manager 用 dir helper 写入新路径

**Files:**
- Modify: `backend/pkg/mcp/manager.go:50-55, 87-92`
- Modify: `backend/pkg/mcp/manager_test.go`

- [ ] **Step 1: 先看 manager_test 当前断言**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && grep -n 'mcp' backend/pkg/mcp/manager_test.go | grep -i 'filepath\|RootDir\|dir,'
```

预期：能看到形如 `filepath.Join(dataDir, "mcp", ...)` 的断言行号。记住它们待会儿要改。

- [ ] **Step 2: 把 manager.go 里硬编码的 "mcp" / "plugin" 改成 helper**

在 `backend/pkg/mcp/manager.go` 顶部 import 中加：

```go
import (
	// ... 现有 import ...
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/dir"
)
```

把 `ImportMCP` 函数里：

```go
targetDir := filepath.Join(m.dataDir, "mcp", manifest.Name, versionDir)
```

改为：

```go
targetDir := filepath.Join(dir.MCPRoot(m.dataDir), manifest.Name, versionDir)
```

把 `ImportPlugin` 函数里：

```go
targetDir := filepath.Join(m.dataDir, "plugin", manifest.Name, versionDir)
```

改为：

```go
targetDir := filepath.Join(dir.PluginRoot(m.dataDir), manifest.Name, versionDir)
```

- [ ] **Step 3: 更新 manager_test.go 中所有路径断言**

`backend/pkg/mcp/manager_test.go` 现有断言使用 `strings.Contains(item.RootDir, filepath.Join("mcp", ...))`，新路径仍然包含这段子串所以测试不会失败，但断言力度不够。改成强校验：

* 第 116 行附近：
  ```go
  if !strings.Contains(item.RootDir, filepath.Join("mcp", "filesystem", "1.2.3")) {
  ```
  改为：
  ```go
  if !strings.Contains(item.RootDir, filepath.Join("plugins", "mcp", "filesystem", "1.2.3")) {
  ```

* 第 138 行附近：
  ```go
  if !strings.Contains(item.RootDir, filepath.Join("mcp", "clock", "default")) {
  ```
  改为：
  ```go
  if !strings.Contains(item.RootDir, filepath.Join("plugins", "mcp", "clock", "default")) {
  ```

* 同文件里若还有针对 `ImportPlugin` 的类似断言（搜 `filepath.Join("plugin"`），按同样模式加上 `"plugins"` 前缀。

- [ ] **Step 4: 跑 mcp 包全部测试**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/pkg/mcp/...
```

预期：PASS。

- [ ] **Step 5: 提交**

```bash
git add backend/pkg/mcp/
git commit -m "refactor(mcp): write imports under plugins/mcp and plugins/plugin via dir helper"
```

---

## Task 3: migration 包 — 幂等标记 + 目录搬迁（TDD）

**Files:**
- Create: `backend/pkg/migration/extension_dirs.go`
- Create: `backend/pkg/migration/extension_dirs_test.go`

- [ ] **Step 1: 写测试文件骨架与第一个测试**

新建 `backend/pkg/migration/extension_dirs_test.go`，内容如下：

```go
package migration

import (
	"encoding/json"
	"os"
	"path/filepath"
	"testing"
)

// writeConfigJSON helper writes a minimal config.json with the given Extensions slice for migration tests.
func writeConfigJSON(t *testing.T, dataDir string, extensions []map[string]any) {
	t.Helper()
	cfg := map[string]any{
		"data_dir":   dataDir,
		"extensions": extensions,
	}
	bytes, err := json.MarshalIndent(cfg, "", "  ")
	if err != nil {
		t.Fatalf("marshal config: %v", err)
	}
	if err := os.WriteFile(filepath.Join(dataDir, "config.json"), bytes, 0o644); err != nil {
		t.Fatalf("write config: %v", err)
	}
}

// readConfigJSON helper loads back config.json as a generic map.
func readConfigJSON(t *testing.T, dataDir string) map[string]any {
	t.Helper()
	bytes, err := os.ReadFile(filepath.Join(dataDir, "config.json"))
	if err != nil {
		t.Fatalf("read config: %v", err)
	}
	out := map[string]any{}
	if err := json.Unmarshal(bytes, &out); err != nil {
		t.Fatalf("unmarshal: %v", err)
	}
	return out
}

// TestMigrateExtensionDirs_NoLegacyNoOp verifies migration is a no-op when neither legacy dir exists.
func TestMigrateExtensionDirs_NoLegacyNoOp(t *testing.T) {
	dataDir := t.TempDir()
	writeConfigJSON(t, dataDir, nil)

	result, err := MigrateExtensionDirs(dataDir)
	if err != nil {
		t.Fatalf("migrate: %v", err)
	}
	if result.MovedMCP || result.MovedPlugin {
		t.Fatalf("expected no moves, got %+v", result)
	}

	markerPath := filepath.Join(dataDir, "plugins", ".migrated_v1")
	if _, err := os.Stat(markerPath); err != nil {
		t.Fatalf("expected marker written even on no-op, got %v", err)
	}
}
```

- [ ] **Step 2: 跑测试确认失败**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/pkg/migration/ -v
```

预期：编译失败（`undefined: MigrateExtensionDirs`）。

- [ ] **Step 3: 写最小实现让第一个测试通过**

新建 `backend/pkg/migration/extension_dirs.go`：

```go
// Package migration handles one-time data directory layout migrations between Lemontea releases.
package migration

import (
	"encoding/json"
	"errors"
	"os"
	"path/filepath"

	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/dir"
)

// markerFileName is the idempotency marker written under the plugins root after a successful migration.
const markerFileName = ".migrated_v1"

// MigrationResult summarizes what happened during a single migration run.
type MigrationResult struct {
	AlreadyMigrated bool
	MovedMCP        bool
	MovedPlugin     bool
	RewroteConfig   bool
	Warnings        []string
}

// MigrateExtensionDirs performs a one-time migration of legacy mcp/ and plugin/ dirs into plugins/.
// It is safe to call on every startup: an idempotency marker prevents repeat work.
func MigrateExtensionDirs(dataDir string) (MigrationResult, error) {
	result := MigrationResult{}

	if err := os.MkdirAll(dir.ExtensionsRoot(dataDir), 0o755); err != nil {
		return result, err
	}

	markerPath := filepath.Join(dir.ExtensionsRoot(dataDir), markerFileName)
	if _, err := os.Stat(markerPath); err == nil {
		result.AlreadyMigrated = true
		return result, nil
	} else if !errors.Is(err, os.ErrNotExist) {
		return result, err
	}

	// Step 1: move legacy dirs if present.
	if moved, warn, err := moveLegacy(dir.LegacyMCPRoot(dataDir), dir.MCPRoot(dataDir)); err != nil {
		return result, err
	} else {
		result.MovedMCP = moved
		if warn != "" {
			result.Warnings = append(result.Warnings, warn)
		}
	}
	if moved, warn, err := moveLegacy(dir.LegacyPluginRoot(dataDir), dir.PluginRoot(dataDir)); err != nil {
		return result, err
	} else {
		result.MovedPlugin = moved
		if warn != "" {
			result.Warnings = append(result.Warnings, warn)
		}
	}

	// Step 2: rewrite config paths if any move happened.
	if result.MovedMCP || result.MovedPlugin {
		rewrote, err := rewriteConfigPaths(dataDir)
		if err != nil {
			return result, err
		}
		result.RewroteConfig = rewrote
	}

	// Step 3: write idempotency marker.
	if err := os.WriteFile(markerPath, []byte("v1"), 0o644); err != nil {
		return result, err
	}
	return result, nil
}

// moveLegacy moves srcDir into dstDir when srcDir exists and dstDir does not.
// It returns (moved, warning, error). When both exist we keep srcDir and log via warning.
func moveLegacy(srcDir string, dstDir string) (bool, string, error) {
	srcInfo, err := os.Stat(srcDir)
	if err != nil {
		if errors.Is(err, os.ErrNotExist) {
			return false, "", nil
		}
		return false, "", err
	}
	if !srcInfo.IsDir() {
		return false, "legacy path is not a directory: " + srcDir, nil
	}

	if _, err := os.Stat(dstDir); err == nil {
		return false, "both legacy and new dirs exist, leaving as-is: " + srcDir, nil
	} else if !errors.Is(err, os.ErrNotExist) {
		return false, "", err
	}

	if err := os.MkdirAll(filepath.Dir(dstDir), 0o755); err != nil {
		return false, "", err
	}
	if err := os.Rename(srcDir, dstDir); err != nil {
		return false, "", err
	}
	return true, "", nil
}

// rewriteConfigPaths loads config.json, replaces extension dir prefixes, and writes the file back.
// It returns true when at least one ExtensionItem path was rewritten.
func rewriteConfigPaths(dataDir string) (bool, error) {
	configPath := filepath.Join(dataDir, "config.json")
	bytes, err := os.ReadFile(configPath)
	if err != nil {
		if errors.Is(err, os.ErrNotExist) {
			return false, nil
		}
		return false, err
	}

	var cfg map[string]any
	if err := json.Unmarshal(bytes, &cfg); err != nil {
		return false, err
	}

	extensionsAny, ok := cfg["extensions"].([]any)
	if !ok {
		return false, nil
	}

	rewrote := false
	for _, raw := range extensionsAny {
		item, ok := raw.(map[string]any)
		if !ok {
			continue
		}
		if rewriteOneField(item, "root_dir", dataDir) {
			rewrote = true
		}
		if rewriteOneField(item, "config_file_path", dataDir) {
			rewrote = true
		}
	}
	if !rewrote {
		return false, nil
	}

	updated, err := json.MarshalIndent(cfg, "", "  ")
	if err != nil {
		return false, err
	}
	if err := os.WriteFile(configPath, updated, 0o644); err != nil {
		return false, err
	}
	return true, nil
}

// rewriteOneField replaces legacy "{dataDir}/mcp/" or "{dataDir}/plugin/" prefixes
// with the new "{dataDir}/plugins/mcp/" or "{dataDir}/plugins/plugin/" prefix.
func rewriteOneField(item map[string]any, key string, dataDir string) bool {
	value, ok := item[key].(string)
	if !ok || value == "" {
		return false
	}
	legacyMCP := dir.LegacyMCPRoot(dataDir) + string(filepath.Separator)
	legacyPlugin := dir.LegacyPluginRoot(dataDir) + string(filepath.Separator)

	switch {
	case len(value) > len(legacyMCP) && value[:len(legacyMCP)] == legacyMCP:
		item[key] = dir.MCPRoot(dataDir) + string(filepath.Separator) + value[len(legacyMCP):]
		return true
	case len(value) > len(legacyPlugin) && value[:len(legacyPlugin)] == legacyPlugin:
		item[key] = dir.PluginRoot(dataDir) + string(filepath.Separator) + value[len(legacyPlugin):]
		return true
	}
	return false
}
```

- [ ] **Step 4: 跑测试确认通过**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/pkg/migration/ -v
```

预期：PASS。

- [ ] **Step 5: 追加更多场景测试**

在 `extension_dirs_test.go` 末尾追加：

```go
// TestMigrateExtensionDirs_MovesLegacyMCP verifies a legacy mcp/ dir is moved to plugins/mcp/.
func TestMigrateExtensionDirs_MovesLegacyMCP(t *testing.T) {
	dataDir := t.TempDir()
	writeConfigJSON(t, dataDir, []map[string]any{
		{
			"id":               "mcp:demo:default",
			"kind":             "mcp",
			"root_dir":         filepath.Join(dataDir, "mcp", "demo", "default"),
			"config_file_path": filepath.Join(dataDir, "mcp", "demo", "default", "mcp.json"),
		},
	})

	legacyDir := filepath.Join(dataDir, "mcp", "demo", "default")
	if err := os.MkdirAll(legacyDir, 0o755); err != nil {
		t.Fatal(err)
	}
	if err := os.WriteFile(filepath.Join(legacyDir, "marker.txt"), []byte("x"), 0o644); err != nil {
		t.Fatal(err)
	}

	result, err := MigrateExtensionDirs(dataDir)
	if err != nil {
		t.Fatalf("migrate: %v", err)
	}
	if !result.MovedMCP || !result.RewroteConfig {
		t.Fatalf("expected MovedMCP and RewroteConfig, got %+v", result)
	}

	newPath := filepath.Join(dataDir, "plugins", "mcp", "demo", "default", "marker.txt")
	if _, err := os.Stat(newPath); err != nil {
		t.Fatalf("expected file moved to %s, got %v", newPath, err)
	}

	cfg := readConfigJSON(t, dataDir)
	extensions := cfg["extensions"].([]any)
	first := extensions[0].(map[string]any)
	expectedRoot := filepath.Join(dataDir, "plugins", "mcp", "demo", "default")
	if first["root_dir"] != expectedRoot {
		t.Fatalf("root_dir not rewritten, got %v", first["root_dir"])
	}
	expectedCfg := filepath.Join(expectedRoot, "mcp.json")
	if first["config_file_path"] != expectedCfg {
		t.Fatalf("config_file_path not rewritten, got %v", first["config_file_path"])
	}
}

// TestMigrateExtensionDirs_IdempotentSkipsOnMarker verifies that a second run is a no-op.
func TestMigrateExtensionDirs_IdempotentSkipsOnMarker(t *testing.T) {
	dataDir := t.TempDir()
	writeConfigJSON(t, dataDir, nil)

	if _, err := MigrateExtensionDirs(dataDir); err != nil {
		t.Fatalf("first migrate: %v", err)
	}

	legacyDir := filepath.Join(dataDir, "mcp", "demo", "default")
	if err := os.MkdirAll(legacyDir, 0o755); err != nil {
		t.Fatal(err)
	}

	result, err := MigrateExtensionDirs(dataDir)
	if err != nil {
		t.Fatalf("second migrate: %v", err)
	}
	if !result.AlreadyMigrated {
		t.Fatalf("expected AlreadyMigrated, got %+v", result)
	}
	if _, err := os.Stat(legacyDir); err != nil {
		t.Fatalf("legacy dir should be untouched on second run, got %v", err)
	}
}

// TestMigrateExtensionDirs_BothExistKeepsLegacy verifies both legacy and new co-existing produces a warning.
func TestMigrateExtensionDirs_BothExistKeepsLegacy(t *testing.T) {
	dataDir := t.TempDir()
	writeConfigJSON(t, dataDir, nil)

	if err := os.MkdirAll(filepath.Join(dataDir, "mcp", "x"), 0o755); err != nil {
		t.Fatal(err)
	}
	if err := os.MkdirAll(filepath.Join(dataDir, "plugins", "mcp", "y"), 0o755); err != nil {
		t.Fatal(err)
	}

	result, err := MigrateExtensionDirs(dataDir)
	if err != nil {
		t.Fatalf("migrate: %v", err)
	}
	if result.MovedMCP {
		t.Fatalf("should not move when both exist")
	}
	if len(result.Warnings) == 0 {
		t.Fatalf("expected at least one warning")
	}
	if _, err := os.Stat(filepath.Join(dataDir, "mcp", "x")); err != nil {
		t.Fatalf("legacy x should remain, got %v", err)
	}
}
```

- [ ] **Step 6: 跑全部 migration 测试**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/pkg/migration/ -v
```

预期：四个测试全部 PASS。

- [ ] **Step 7: 提交**

```bash
git add backend/pkg/migration/
git commit -m "feat(migration): add one-time extension dirs migration with idempotency marker"
```

---

## Task 4: 在 main.go 启动流程中调用迁移

**Files:**
- Modify: `main.go`

- [ ] **Step 1: 在 main.go 顶部 import migration 包**

打开 `main.go`，把 import 段中：

```go
"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/storage"
```

下方追加：

```go
"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/dir"
"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/migration"
```

- [ ] **Step 2: 在 storage 初始化之后、`application.New` 之前插入迁移调用**

把 main 函数顶部：

```go
func main() {
	istorage, err := storage.NewStorage()
	if err != nil {
		log.Fatal(err)
	}

	providerService := provider.NewProvider(istorage)
```

改为：

```go
func main() {
	istorage, err := storage.NewStorage()
	if err != nil {
		log.Fatal(err)
	}

	if dataDir, derr := dir.GetDataDir(); derr == nil {
		if result, merr := migration.MigrateExtensionDirs(dataDir); merr != nil {
			log.Printf("extension dir migration failed: %v", merr)
		} else if len(result.Warnings) > 0 {
			for _, w := range result.Warnings {
				log.Printf("extension dir migration warning: %s", w)
			}
		}
	} else {
		log.Printf("skip extension dir migration: cannot resolve data dir: %v", derr)
	}

	providerService := provider.NewProvider(istorage)
```

迁移失败**不阻塞启动**：只记录日志。这是有意为之，避免一次失败的迁移让用户进不去应用。

- [ ] **Step 3: 跑构建确保没有破坏**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go build ./...
```

预期：编译通过，没有 import 错误。

- [ ] **Step 4: 跑全部 Go 测试**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./...
```

预期：全部 PASS。

- [ ] **Step 5: 手工验证（如果当前数据目录恰好有旧布局可选做）**

```bash
ls ~/.lemontea/ 2>&1 | grep -E '^(mcp|plugin|plugins)$'
```

若看到 `mcp` 或 `plugin` 而无 `plugins`，跑一次应用观察是否迁移；若已经是 `plugins`，跳过。

- [ ] **Step 6: 提交**

```bash
git add main.go
git commit -m "feat(main): run extension dir migration on startup"
```

---

## Task 5: 验收清单（执行完上面后人工过一遍）

- [ ] `go test ./...` 全部通过。
- [ ] 干净环境（无 `~/.lemontea/mcp/`、`~/.lemontea/plugin/`）启动应用：日志无 migration 报错；`~/.lemontea/plugins/.migrated_v1` 文件存在。
- [ ] 构造旧布局（手工在 `~/.lemontea/mcp/foo/default/` 放一个文件）后再启动应用：该文件已搬到 `~/.lemontea/plugins/mcp/foo/default/`，`config.json` 中相关条目的 `root_dir` 已更新。
- [ ] 旧 MCP 工具加载与启用流程仍然能跑（在「设置 → 插件与工具」页确认列表 + 启用 + 调用一切如常）。

---

## 不在 P1 范围

* CLI 插件自身（pkg/cli、cli 安装/调用 / UI）— 留给 P2 与 P3。
* `cli_data/` 目录的实际写入与生命周期（迁移阶段只确保 helper 路径准备好，不创建任何 cli_data 子目录）。
