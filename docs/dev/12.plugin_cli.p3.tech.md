# CLI 插件实施计划 — P3 Service + 设置页 UI

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 P2 的 `backend/pkg/cli` 接入 `service/plugin`，新增 Wails 安装/重置/manifest 编辑方法，扩展前端「插件与工具」页：新增「添加 → CLI」二级菜单（从 npm 安装 / 从本地目录导入）、列表项 cli 标签、详情面板支持 manifest 编辑 + 重置数据 + 卸载。本期**不**接 AI 生成 manifest（P4）、**不**接 agent 工具池注册（P4）、**不**实现登录流（P5）、**不**接聊天端 meta tool（P6）。

**Architecture:** Plugin 服务持有 `cli.Manager`，按需从 runtime state.json 读取 node/npm 路径懒构造。CLI 插件复用 `data_models.ExtensionItem`，`kind: "cli"`，`ID = "cli:{name}"`（CLI 不带版本号）。前端按现有 `kind === 'cli'` 分支扩展菜单、列表项标签、详情面板。安装期间同步等待 Wails 调用完成，UI 显示 loading 状态；进度事件流留到后续。

**Tech Stack:** Go + Wails v3 + React + Tailwind + i18next。

参考需求文档：`docs/dev/12.plugin_cli.md` §4 安装入口、§5 调用流程（部分）、§7 UI 设计。所有实现必须遵循 `docs/dev/00.rules.md`。

---

## 文件结构

### 新建文件

```
backend/
  service/
    plugin/
      plugin_cli.go                      # CLI 相关 Wails 方法
      plugin_cli_test.go
      plugin_dto/
        install_cli_from_npm.go
        install_cli_from_local.go
        reset_cli_data.go
        update_cli_manifest.go

frontend/
  src/
    components/
      settings/
        plugins/
          CliInstallModal.tsx            # npm / 本地目录二选一弹窗
          CliExtensionDetailView.tsx     # CLI 详情面板（manifest 编辑 + 危险操作）
```

### 修改文件

```
backend/
  service/
    runtime/
      runtime_internal.go                # 导出 LoadPersistedState 供 plugin 服务使用
      runtime_test.go                    # 调用方更新
  service/
    plugin/
      plugin.go                          # 持有 cli.Manager，extend ImportExtension/Delete 等
      plugin_internal.go                 # syncExtensionRuntime 支持 cli kind
      plugin_test.go                     # 新增 cli 路径回归

frontend/
  src/
    types/settings.ts                    # kind 联合新增 'cli'
    components/settings/plugins/
      PluginToolList.tsx                 # onCreate 增加 'cli'，菜单增加 CLI 子菜单
      PluginToolListItem.tsx             # cli 标签颜色 / 文案
      PluginToolDetailView.tsx           # kind === 'cli' 分支渲染 CliExtensionDetailView
      ExtensionDetailInfo.tsx            # cli kind 显示 InstallDir、CLIDataDir
    i18n/locales/en.ts                   # 新增 cli 相关 key
    i18n/locales/zh-CN.ts                # 新增 cli 相关 key
```

---

## Task 1: runtime 导出 state 读取 helper

**Files:**
- Modify: `backend/service/runtime/runtime_internal.go`
- Modify: `backend/service/runtime/runtime.go`（调用方）

- [ ] **Step 1: 把 `loadState` 重命名为 `LoadPersistedState`（导出）**

`backend/service/runtime/runtime_internal.go` 中查找 `func loadState()` 并改为：

```go
// LoadPersistedState reads the Node runtime state.json. Missing file yields a fresh missing state.
// Exposed so other services (e.g. plugin) can resolve the bundled node/npm paths without depending on the runtime Service.
func LoadPersistedState() (RuntimeState, error) {
	// ... 原 loadState 函数体保持不变 ...
}
```

- [ ] **Step 2: 同文件内所有 `loadState()` 调用改为 `LoadPersistedState()`**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && grep -n 'loadState' backend/service/runtime/
```

预期：列出所有调用点（runtime.go 与 runtime_internal.go 内部）。逐个替换。

- [ ] **Step 3: 跑 runtime 测试**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/service/runtime/ -v
```

预期：全 PASS。

- [ ] **Step 4: 提交**

```bash
git add backend/service/runtime/
git commit -m "refactor(runtime): export LoadPersistedState for cross-service consumption"
```

---

## Task 2: Plugin 服务 — InstallCliFromNpm + InstallCliFromLocal

**Files:**
- Create: `backend/service/plugin/plugin_cli.go`
- Create: `backend/service/plugin/plugin_dto/install_cli_from_npm.go`
- Create: `backend/service/plugin/plugin_dto/install_cli_from_local.go`
- Modify: `backend/service/plugin/plugin.go`（在 `Plugin` 结构体加 `cliManager` 字段 + helper）

- [ ] **Step 1: 新增两个 DTO 文件**

`backend/service/plugin/plugin_dto/install_cli_from_npm.go`:

```go
package plugin_dto

import "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/models/data_models"

// InstallCliFromNpmInput is the request to install a CLI plugin from a published npm package.
type InstallCliFromNpmInput struct {
	NpmPackage string `json:"npm_package"`
	Name       string `json:"name"`
}

// InstallCliFromNpmOutput returns the persisted ExtensionItem after a successful install.
type InstallCliFromNpmOutput struct {
	Extension data_models.ExtensionItem `json:"extension"`
}
```

`backend/service/plugin/plugin_dto/install_cli_from_local.go`:

```go
package plugin_dto

import "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/models/data_models"

// InstallCliFromLocalInput is the request to install a CLI plugin from a local directory containing package.json.
type InstallCliFromLocalInput struct {
	LocalPath string `json:"local_path"`
	Name      string `json:"name"`
}

// InstallCliFromLocalOutput returns the persisted ExtensionItem after a successful install.
type InstallCliFromLocalOutput struct {
	Extension data_models.ExtensionItem `json:"extension"`
}
```

- [ ] **Step 2: 在 `plugin.go` 给 `Plugin` 加 `cliManager` 字段 + lazy 构造 helper**

打开 `backend/service/plugin/plugin.go`，把：

```go
type Plugin struct {
	mcpManager *pkgmcp.Manager
}

func NewPlugin() *Plugin {
	return &Plugin{mcpManager: pkgmcp.NewManager(mustDataDir())}
}
```

替换为：

```go
type Plugin struct {
	mcpManager *pkgmcp.Manager
	cliManager *pkgcli.Manager
}

func NewPlugin() *Plugin {
	return &Plugin{mcpManager: pkgmcp.NewManager(mustDataDir())}
}

// resolveCliManager constructs (lazily) the CLI manager by reading node/npm paths from runtime state.json.
// Returns an error if the runtime is not yet downloaded / ready.
func (p *Plugin) resolveCliManager() (*pkgcli.Manager, error) {
	if p.cliManager != nil {
		return p.cliManager, nil
	}
	state, err := runtimeSvc.LoadPersistedState()
	if err != nil {
		return nil, err
	}
	if state.State != runtimeSvc.StateReady || state.NodePath == "" || state.NpmPath == "" {
		return nil, ierror.Error(ierror.ErrCliRuntimeUnavailable, errors.New("plugin runtime not ready"))
	}
	p.cliManager = pkgcli.NewManager(mustDataDir(), state.NodePath, state.NpmPath)
	return p.cliManager, nil
}
```

需要的新 import（追加到 `plugin.go` 顶部 import 段）：

```go
"errors"
pkgcli "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/cli"
runtimeSvc "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/runtime"
```

- [ ] **Step 3: 新增错误码 `ErrCliRuntimeUnavailable`**

打开 `backend/pkg/ierror/error.go`，在现有 runtime 相关错误下方加：

```go
ErrCliRuntimeUnavailable    errorCode = "ierror.cli.runtime_unavailable"
ErrCliInstallFailed         errorCode = "ierror.cli.install_failed"
ErrCliResetDataFailed       errorCode = "ierror.cli.reset_data_failed"
ErrCliManifestInvalid       errorCode = "ierror.cli.manifest_invalid"
ErrCliManifestSaveFailed    errorCode = "ierror.cli.manifest_save_failed"
```

再到 `backend/pkg/i18n/resources_en_us.go` 与 `resources_en_zh_cn.go` 加对应文案（每个错误码两条文案：英文与中文）。

- [ ] **Step 4: 新建 `backend/service/plugin/plugin_cli.go`**

```go
package plugin

import (
	"context"
	"strings"

	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/models/data_models"
	pkgcli "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/cli"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/ierror"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/plugin/plugin_dto"
)

// InstallCliFromNpm installs a published npm package as a CLI plugin and persists it to config.json.
func (p *Plugin) InstallCliFromNpm(ctx context.Context, input plugin_dto.InstallCliFromNpmInput) (*plugin_dto.InstallCliFromNpmOutput, error) {
	name := pickInstallName(input.Name, input.NpmPackage)
	mgr, err := p.resolveCliManager()
	if err != nil {
		return nil, err
	}
	res, err := mgr.InstallFromNpm(ctx, pkgcli.InstallParams{NpmPackage: input.NpmPackage, Name: name})
	if err != nil {
		return nil, ierror.Error(ierror.ErrCliInstallFailed, err)
	}
	item, err := p.persistCliExtension(res, true)
	if err != nil {
		return nil, err
	}
	return &plugin_dto.InstallCliFromNpmOutput{Extension: item}, nil
}

// InstallCliFromLocal installs a local directory as a CLI plugin and persists it to config.json.
func (p *Plugin) InstallCliFromLocal(ctx context.Context, input plugin_dto.InstallCliFromLocalInput) (*plugin_dto.InstallCliFromLocalOutput, error) {
	name := pickInstallName(input.Name, defaultNameFromPath(input.LocalPath))
	mgr, err := p.resolveCliManager()
	if err != nil {
		return nil, err
	}
	res, err := mgr.InstallFromLocal(ctx, pkgcli.InstallParams{LocalPath: input.LocalPath, Name: name})
	if err != nil {
		return nil, ierror.Error(ierror.ErrCliInstallFailed, err)
	}
	item, err := p.persistCliExtension(res, true)
	if err != nil {
		return nil, err
	}
	return &plugin_dto.InstallCliFromLocalOutput{Extension: item}, nil
}

// pickInstallName returns the user-provided name when non-empty, else falls back to the npm package / dir basename.
func pickInstallName(provided string, fallback string) string {
	if v := strings.TrimSpace(provided); v != "" {
		return sanitizeName(v)
	}
	return sanitizeName(fallback)
}

// defaultNameFromPath extracts the basename for a local source path.
func defaultNameFromPath(p string) string {
	for i := len(p) - 1; i >= 0; i-- {
		if p[i] == '/' || p[i] == '\\' {
			return p[i+1:]
		}
	}
	return p
}

// sanitizeName strips characters not allowed in directory names. We only allow [a-zA-Z0-9_.-].
func sanitizeName(s string) string {
	var b strings.Builder
	for _, r := range s {
		switch {
		case r >= 'a' && r <= 'z', r >= 'A' && r <= 'Z', r >= '0' && r <= '9', r == '_', r == '-', r == '.':
			b.WriteRune(r)
		}
	}
	if b.Len() == 0 {
		return "cli-plugin"
	}
	return b.String()
}

// persistCliExtension converts an cli.InstallResult into an ExtensionItem and writes it to config.json.
// enabled controls the initial Enabled flag on the persisted item.
func (p *Plugin) persistCliExtension(res pkgcli.InstallResult, enabled bool) (data_models.ExtensionItem, error) {
	config, err := p.loadConfig()
	if err != nil {
		return data_models.ExtensionItem{}, ierror.Error(ierror.ErrSettingsLoadConfig, err)
	}
	item := data_models.ExtensionItem{
		ID:             "cli:" + res.Name,
		Name:           res.Name,
		Description:    res.Description,
		Author:         res.Author,
		Version:        res.Version,
		Kind:           "cli",
		Enabled:        enabled,
		RuntimeStatus:  "ready",
		RuntimeMessage: "",
		RootDir:        res.InstallDir,
		SourceDir:      "",
		ConfigFilePath: res.ManifestPath,
		Tools:          []data_models.ExtensionTool{},
	}
	config.Extensions = append(removeExtension(config.Extensions, item.ID), item)
	if err := p.saveConfig(config); err != nil {
		return data_models.ExtensionItem{}, ierror.Error(ierror.ErrSettingsSaveConfig, err)
	}
	return item, nil
}
```

- [ ] **Step 5: 写测试 `backend/service/plugin/plugin_cli_test.go`**

```go
package plugin

import (
	"context"
	"os"
	"path/filepath"
	"runtime"
	"testing"

	pkgcli "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/pkg/cli"
	"gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/service/plugin/plugin_dto"
)

// withFakeCliManager swaps in a Manager whose npm is mocked to install a synthetic lark-cli bundle.
// It also points the data dir at a tempdir via LEMONTEA_DATA_DIR. Returns the dataDir for assertions.
func withFakeCliManager(t *testing.T, p *Plugin) string {
	t.Helper()
	if runtime.GOOS == "windows" {
		t.Skip("plugin cli tests use POSIX shell mocks")
	}
	dataDir := t.TempDir()
	t.Setenv("LEMONTEA_DATA_DIR", dataDir)

	binDir := t.TempDir()
	npmPath := filepath.Join(binDir, "npm")
	nodePath := filepath.Join(binDir, "node")
	if err := os.WriteFile(nodePath, []byte("#!/usr/bin/env bash\nexit 0\n"), 0o755); err != nil {
		t.Fatal(err)
	}
	npmBody := `#!/usr/bin/env bash
target=""
seen_prefix=0
for a in "$@"; do
  if [ "$seen_prefix" = "1" ]; then target="$a"; seen_prefix=0; fi
  if [ "$a" = "--prefix" ]; then seen_prefix=1; fi
done
mkdir -p "$target/node_modules/.bin"
mkdir -p "$target/node_modules/lark-cli"
cat > "$target/node_modules/lark-cli/package.json" <<EOF
{"name":"lark-cli","version":"1.2.3","description":"feishu","bin":"lark.js"}
EOF
cat > "$target/node_modules/.bin/lark-cli" <<EOF
#!/usr/bin/env bash
exit 0
EOF
chmod +x "$target/node_modules/.bin/lark-cli"
exit 0
`
	if err := os.WriteFile(npmPath, []byte(npmBody), 0o755); err != nil {
		t.Fatal(err)
	}
	p.cliManager = pkgcli.NewManager(dataDir, nodePath, npmPath)
	return dataDir
}

// TestInstallCliFromNpmPersistsExtension verifies a successful install creates an ExtensionItem with kind="cli".
func TestInstallCliFromNpmPersistsExtension(t *testing.T) {
	p := NewPlugin()
	dataDir := withFakeCliManager(t, p)
	out, err := p.InstallCliFromNpm(context.Background(), plugin_dto.InstallCliFromNpmInput{NpmPackage: "lark-cli", Name: "lark-cli"})
	if err != nil {
		t.Fatalf("install: %v", err)
	}
	if out.Extension.Kind != "cli" || out.Extension.ID != "cli:lark-cli" {
		t.Fatalf("ext: %+v", out.Extension)
	}
	if _, err := os.Stat(filepath.Join(dataDir, "plugins", "cli", "lark-cli", "node_modules", ".bin", "lark-cli")); err != nil {
		t.Fatalf("bin missing: %v", err)
	}
	// Config should have one extension persisted.
	cfg, err := p.loadConfig()
	if err != nil {
		t.Fatal(err)
	}
	if len(cfg.Extensions) != 1 {
		t.Fatalf("extensions: %+v", cfg.Extensions)
	}
}

// TestInstallCliFromLocalPersistsExtension verifies the local-path variant produces the same persisted item.
func TestInstallCliFromLocalPersistsExtension(t *testing.T) {
	p := NewPlugin()
	withFakeCliManager(t, p)
	src := t.TempDir()
	if err := os.WriteFile(filepath.Join(src, "package.json"), []byte(`{"name":"src"}`), 0o644); err != nil {
		t.Fatal(err)
	}
	out, err := p.InstallCliFromLocal(context.Background(), plugin_dto.InstallCliFromLocalInput{LocalPath: src, Name: "lark-cli"})
	if err != nil {
		t.Fatalf("install: %v", err)
	}
	if out.Extension.ID != "cli:lark-cli" {
		t.Fatalf("ID: %q", out.Extension.ID)
	}
}
```

- [ ] **Step 6: 跑测试**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/service/plugin/ -v
```

预期：所有测试 PASS（含新增 2 个）。

- [ ] **Step 7: 提交**

```bash
git add backend/service/plugin/ backend/pkg/ierror/ backend/pkg/i18n/
git commit -m "feat(plugin): add InstallCliFromNpm and InstallCliFromLocal Wails methods"
```

---

## Task 3: Plugin 服务 — ResetCliData + UpdateCliManifest

**Files:**
- Modify: `backend/service/plugin/plugin_cli.go`（追加）
- Create: `backend/service/plugin/plugin_dto/reset_cli_data.go`
- Create: `backend/service/plugin/plugin_dto/update_cli_manifest.go`
- Modify: `backend/service/plugin/plugin_cli_test.go`（追加）

- [ ] **Step 1: 新增两个 DTO 文件**

`reset_cli_data.go`:

```go
package plugin_dto

// ResetCliDataInput identifies which CLI plugin's data directory to clear.
type ResetCliDataInput struct {
	ID string `json:"id"`
}

// ResetCliDataOutput is empty; clients re-fetch the extension if they need updated state.
type ResetCliDataOutput struct{}
```

`update_cli_manifest.go`:

```go
package plugin_dto

import "gitlab.linhf.cn/project/lemontea/lemon_tea_desktop/backend/models/data_models"

// UpdateCliManifestInput carries the edited manifest JSON text for a specific CLI plugin.
type UpdateCliManifestInput struct {
	ID           string `json:"id"`
	ManifestText string `json:"manifest_text"`
}

// UpdateCliManifestOutput returns the updated ExtensionItem after manifest validation + save.
type UpdateCliManifestOutput struct {
	Extension data_models.ExtensionItem `json:"extension"`
}
```

- [ ] **Step 2: 追加 Wails 方法到 `plugin_cli.go`**

```go
// ResetCliData removes the cli_data/<name> subtree for the given CLI plugin (login state, manifest, XDG dirs).
func (p *Plugin) ResetCliData(ctx context.Context, input plugin_dto.ResetCliDataInput) (*plugin_dto.ResetCliDataOutput, error) {
	name, err := cliNameFromID(input.ID)
	if err != nil {
		return nil, err
	}
	mgr, err := p.resolveCliManager()
	if err != nil {
		return nil, err
	}
	if err := mgr.ResetData(ctx, name); err != nil {
		return nil, ierror.Error(ierror.ErrCliResetDataFailed, err)
	}
	return &plugin_dto.ResetCliDataOutput{}, nil
}

// UpdateCliManifest validates the submitted JSON, writes it via cli.SaveManifest, and refreshes the persisted ExtensionItem.
func (p *Plugin) UpdateCliManifest(ctx context.Context, input plugin_dto.UpdateCliManifestInput) (*plugin_dto.UpdateCliManifestOutput, error) {
	_ = ctx
	config, err := p.loadConfig()
	if err != nil {
		return nil, ierror.Error(ierror.ErrSettingsLoadConfig, err)
	}
	item, ok := findExtension(config.Extensions, input.ID)
	if !ok || item.Kind != "cli" {
		return nil, ierror.Error(ierror.ErrSettingsReadConfig, os.ErrNotExist)
	}
	var manifest pkgcli.Manifest
	if err := json.Unmarshal([]byte(input.ManifestText), &manifest); err != nil {
		return nil, ierror.Error(ierror.ErrCliManifestInvalid, err)
	}
	if err := pkgcli.Validate(manifest); err != nil {
		return nil, ierror.Error(ierror.ErrCliManifestInvalid, err)
	}
	if err := pkgcli.SaveManifest(item.ConfigFilePath, manifest); err != nil {
		return nil, ierror.Error(ierror.ErrCliManifestSaveFailed, err)
	}
	// Mirror the manifest's name/version/description back onto the persisted item so the list view stays in sync.
	item.Name = manifest.Name
	item.Version = manifest.Version
	item.Description = manifest.Description
	item.Tools = []data_models.ExtensionTool{} // P4 will populate this from manifest.Tools
	config.Extensions = append(removeExtension(config.Extensions, item.ID), item)
	if err := p.saveConfig(config); err != nil {
		return nil, ierror.Error(ierror.ErrSettingsSaveConfig, err)
	}
	return &plugin_dto.UpdateCliManifestOutput{Extension: item}, nil
}

// cliNameFromID parses "cli:<name>" -> "<name>"; returns an error for non-cli IDs.
func cliNameFromID(id string) (string, error) {
	const prefix = "cli:"
	if len(id) <= len(prefix) || id[:len(prefix)] != prefix {
		return "", ierror.Error(ierror.ErrSettingsReadConfig, os.ErrInvalid)
	}
	return id[len(prefix):], nil
}
```

需要追加的 import：`encoding/json`、`os`。

- [ ] **Step 3: 追加测试**

```go
// TestResetCliDataClearsDirectory verifies ResetCliData removes the cli_data/<name>/ subtree.
func TestResetCliDataClearsDirectory(t *testing.T) {
	p := NewPlugin()
	dataDir := withFakeCliManager(t, p)
	if _, err := p.InstallCliFromNpm(context.Background(), plugin_dto.InstallCliFromNpmInput{NpmPackage: "lark-cli", Name: "lark-cli"}); err != nil {
		t.Fatal(err)
	}
	if _, err := p.ResetCliData(context.Background(), plugin_dto.ResetCliDataInput{ID: "cli:lark-cli"}); err != nil {
		t.Fatal(err)
	}
	if _, err := os.Stat(filepath.Join(dataDir, "plugins", "cli_data", "lark-cli")); !os.IsNotExist(err) {
		t.Fatalf("cli_data should be removed: %v", err)
	}
}

// TestUpdateCliManifestPersistsAndSyncs verifies UpdateCliManifest writes the file and updates the persisted item.
func TestUpdateCliManifestPersistsAndSyncs(t *testing.T) {
	p := NewPlugin()
	dataDir := withFakeCliManager(t, p)
	if _, err := p.InstallCliFromNpm(context.Background(), plugin_dto.InstallCliFromNpmInput{NpmPackage: "lark-cli", Name: "lark-cli"}); err != nil {
		t.Fatal(err)
	}
	newManifest := `{
		"name":"lark-cli",
		"version":"9.9.9",
		"description":"edited",
		"executable":"` + filepath.Join(dataDir, "plugins", "cli", "lark-cli", "node_modules", ".bin", "lark-cli") + `",
		"isolation":"isolated",
		"tools":[]
	}`
	out, err := p.UpdateCliManifest(context.Background(), plugin_dto.UpdateCliManifestInput{ID: "cli:lark-cli", ManifestText: newManifest})
	if err != nil {
		t.Fatalf("update: %v", err)
	}
	if out.Extension.Version != "9.9.9" || out.Extension.Description != "edited" {
		t.Fatalf("ext: %+v", out.Extension)
	}
}
```

- [ ] **Step 4: 跑测试 + 提交**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/service/plugin/ -v
git add backend/service/plugin/
git commit -m "feat(plugin): add ResetCliData and UpdateCliManifest Wails methods"
```

---

## Task 4: Plugin 服务 — 扩展现有方法支持 cli kind

**Files:**
- Modify: `backend/service/plugin/plugin.go`（`GetExtensionDetail` / `SaveExtensionConfig`）
- Modify: `backend/service/plugin/plugin_internal.go`（`syncExtensionRuntime`）

- [ ] **Step 1: `syncExtensionRuntime` 增加 cli 分支**

在 `plugin_internal.go` 的 `syncExtensionRuntime` 函数顶部，将原本仅 mcp 分支的判定改为：

```go
if item.Kind == "cli" {
	if _, err := os.Stat(item.ConfigFilePath); err == nil {
		item.RuntimeStatus = "ready"
		item.RuntimeMessage = ""
	} else {
		item.RuntimeStatus = "error"
		item.RuntimeMessage = "manifest missing; reinstall"
	}
	// Tool registration deferred to P4.
	item.Tools = []data_models.ExtensionTool{}
	return item
}
```

放在 `if item.Kind != "mcp"` 分支之前。

- [ ] **Step 2: `GetExtensionDetail` — cli 走 manifest 文件**

打开 `plugin.go` 的 `GetExtensionDetail`，现有代码：

```go
if item.ConfigFilePath == "" {
	return &plugin_dto.GetExtensionDetailOutput{
		Extension:  item,
		ConfigText: "",
	}, nil
}
bytes, err := os.ReadFile(item.ConfigFilePath)
```

CLI 安装时 ConfigFilePath 已被设为 manifest.json 路径，复用现有逻辑。无需改动 GetExtensionDetail —— 但 manifest 文件**可能不存在**（用户用旧 cli_data 跑了重置数据），需要把读取错误改成返回空字符串：

```go
bytes, err := os.ReadFile(item.ConfigFilePath)
if err != nil {
	if errors.Is(err, os.ErrNotExist) {
		return &plugin_dto.GetExtensionDetailOutput{Extension: item, ConfigText: ""}, nil
	}
	return nil, ierror.Error(ierror.ErrSettingsReadConfig, err)
}
```

需要 import `"errors"`（若尚未 import）。

- [ ] **Step 3: `SaveExtensionConfig` — cli 走 manifest 校验路径**

现有 `SaveExtensionConfig` 对所有 ConfigFilePath 不空的 item 直接 `os.WriteFile`，这对 mcp 是 mcp.json，对 cli 是 manifest.json。直接写文本会绕过 manifest validate。改成：

```go
if item.Kind == "cli" {
	// Route through UpdateCliManifest's validation logic to keep both call sites consistent.
	return p.UpdateCliManifest(ctx, plugin_dto.UpdateCliManifestInput{ID: input.ID, ManifestText: input.ConfigText}).
		thenWrap(out, input)  // pseudocode; see actual impl below
}
```

实际写法（避免假代码）：在 `SaveExtensionConfig` 顶部判断 `item.Kind == "cli"` 时直接调用 `UpdateCliManifest` 并转换返回：

```go
if item.Kind == "cli" {
	out, err := p.UpdateCliManifest(ctx, plugin_dto.UpdateCliManifestInput{ID: input.ID, ManifestText: input.ConfigText})
	if err != nil {
		return nil, err
	}
	return &plugin_dto.SaveExtensionConfigOutput{Extension: out.Extension}, nil
}
```

- [ ] **Step 4: `DeleteExtension` — cli 卸载只删 install 目录**

`DeleteExtension` 现有逻辑 `os.RemoveAll(item.RootDir)` 删的是 install 目录，对 cli 来说就是 `plugins/cli/<name>/`，不会触及 `cli_data/<name>/`。无需修改 —— 但加一个 doc 注释说明：

```go
// DeleteExtension removes one imported extension from config and deletes its install directory.
// For CLI plugins this preserves plugins/cli_data/<name>/ (login state) by design; use ResetCliData to clear it.
```

- [ ] **Step 5: 测试**

`plugin_test.go` 中追加：

```go
// TestSaveExtensionConfigRoutesCliThroughManifestValidation verifies invalid JSON for a cli kind fails fast.
func TestSaveExtensionConfigRoutesCliThroughManifestValidation(t *testing.T) {
	p := NewPlugin()
	withFakeCliManager(t, p)
	if _, err := p.InstallCliFromNpm(context.Background(), plugin_dto.InstallCliFromNpmInput{NpmPackage: "lark-cli", Name: "lark-cli"}); err != nil {
		t.Fatal(err)
	}
	_, err := p.SaveExtensionConfig(context.Background(), plugin_dto.SaveExtensionConfigInput{
		ID:         "cli:lark-cli",
		ConfigText: `{ invalid json`,
	})
	if err == nil {
		t.Fatal("expected validation error for invalid manifest")
	}
}
```

- [ ] **Step 6: 跑全部 plugin 测试 + 提交**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./backend/service/plugin/ -v
git add backend/service/plugin/
git commit -m "feat(plugin): route cli kind through manifest validation and runtime status"
```

---

## Task 5: 前端 — types + Add 菜单扩展

**Files:**
- Modify: `frontend/src/types/settings.ts`
- Modify: `frontend/src/components/settings/plugins/PluginToolList.tsx`
- Modify: `frontend/src/components/settings/plugins/PluginToolListItem.tsx`

- [ ] **Step 1: 扩展 `ExtensionItem.kind` 联合**

在 `frontend/src/types/settings.ts` 找到：

```ts
kind: 'mcp' | 'plugin'
```

改为：

```ts
kind: 'mcp' | 'plugin' | 'cli'
```

- [ ] **Step 2: `PluginToolList` 增加 CLI 子菜单**

把 `onCreate` 的类型签名 `(kind: 'mcp' | 'plugin') => void` 改成 `(kind: 'mcp' | 'plugin' | 'cli') => void`。

在 add 菜单内（mcp / plugin 按钮之后）追加一个嵌套按钮——CLI 有二级选项。为简化，CLI 折叠为单按钮 `Add CLI`，点击后弹一个独立 modal（modal 内部再选 npm / local）。在菜单 JSX 中追加：

```tsx
<button
  type="button"
  className="flex w-full items-center px-3 py-2 text-left text-sm text-foreground transition-colors hover:bg-accent"
  onClick={() => {
    props.onCreate('cli')
    setMenuOpen(false)
  }}
>
  {t('settingsPage.plugins.addCli')}
</button>
```

- [ ] **Step 3: `PluginToolListItem` 增加 cli 标签**

定位组件内当前对 `kind === 'mcp'` / `kind === 'plugin'` 标签颜色判断的部分，扩展为三分支：

```tsx
{item.kind === 'cli'
  ? t('settingsPage.plugins.kindCli')
  : item.kind === 'mcp'
  ? t('settingsPage.plugins.kindMcp')
  : t('settingsPage.plugins.kindPlugin')}
```

颜色样式：cli 用紫色（`bg-purple-100 text-purple-700 dark:bg-purple-500/20 dark:text-purple-400`），与 mcp（蓝）/plugin（绿）区分。

- [ ] **Step 4: TypeScript 编译 + 提交**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop/frontend && npx tsc --noEmit
git add frontend/src/types/ frontend/src/components/settings/plugins/PluginToolList.tsx frontend/src/components/settings/plugins/PluginToolListItem.tsx
git commit -m "feat(plugins-ui): add 'cli' kind to add menu and list item badge"
```

---

## Task 6: 前端 — CliInstallModal

**Files:**
- Create: `frontend/src/components/settings/plugins/CliInstallModal.tsx`

- [ ] **Step 1: 写组件**

```tsx
import { useState } from 'react'
import { useTranslation } from 'react-i18next'
import { OpenDirectoryDialog } from '@bindings/.../service/file'  // adjust to actual binding path
import { InstallCliFromNpm, InstallCliFromLocal } from '@bindings/.../service/plugin'
import type { ExtensionItem } from '@/types/settings'

type Tab = 'npm' | 'local'

export function CliInstallModal(props: {
  open: boolean
  onClose: () => void
  onInstalled: (item: ExtensionItem) => void
}) {
  const { t } = useTranslation()
  const [tab, setTab] = useState<Tab>('npm')
  const [npmPkg, setNpmPkg] = useState('')
  const [localPath, setLocalPath] = useState('')
  const [name, setName] = useState('')
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState('')

  if (!props.open) return null

  const pickFolder = async () => {
    const dir = await OpenDirectoryDialog({})  // verify binding name + payload shape
    if (dir) setLocalPath(dir)
  }

  const install = async () => {
    setBusy(true); setError('')
    try {
      let res
      if (tab === 'npm') {
        res = await InstallCliFromNpm({ npm_package: npmPkg, name })
      } else {
        res = await InstallCliFromLocal({ local_path: localPath, name })
      }
      if (res?.extension) props.onInstalled(res.extension)
      props.onClose()
    } catch (e: unknown) {
      setError(String(e))
    } finally {
      setBusy(false)
    }
  }

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40">
      <div className="w-full max-w-md rounded-2xl bg-popover p-6 shadow-xl">
        <h3 className="mb-4 text-lg font-semibold">{t('settingsPage.plugins.cliInstallTitle')}</h3>

        <div className="mb-3 flex gap-2">
          <button onClick={() => setTab('npm')} className={tab === 'npm' ? 'btn-active' : 'btn-inactive'}>
            {t('settingsPage.plugins.cliFromNpm')}
          </button>
          <button onClick={() => setTab('local')} className={tab === 'local' ? 'btn-active' : 'btn-inactive'}>
            {t('settingsPage.plugins.cliFromLocal')}
          </button>
        </div>

        {tab === 'npm' ? (
          <input value={npmPkg} onChange={(e) => setNpmPkg(e.target.value)}
            placeholder={t('settingsPage.plugins.cliNpmPlaceholder')}
            className="mb-3 w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />
        ) : (
          <div className="mb-3 flex gap-2">
            <input value={localPath} readOnly placeholder={t('settingsPage.plugins.cliLocalPlaceholder')}
              className="flex-1 rounded-lg border border-border bg-background px-3 py-2 text-sm" />
            <button type="button" onClick={pickFolder} className="rounded-lg border border-border px-3 text-sm">
              {t('settingsPage.plugins.browse')}
            </button>
          </div>
        )}

        <input value={name} onChange={(e) => setName(e.target.value)}
          placeholder={t('settingsPage.plugins.cliNamePlaceholder')}
          className="mb-3 w-full rounded-lg border border-border bg-background px-3 py-2 text-sm" />

        {error && <div className="mb-3 rounded-lg bg-red-100 px-3 py-2 text-sm text-red-700">{error}</div>}

        <div className="flex justify-end gap-2">
          <button type="button" onClick={props.onClose} disabled={busy}
            className="rounded-lg px-4 py-2 text-sm text-muted-foreground hover:bg-accent">
            {t('common.cancel')}
          </button>
          <button type="button" onClick={install}
            disabled={busy || (tab === 'npm' ? !npmPkg : !localPath)}
            className="rounded-lg bg-primary px-4 py-2 text-sm text-primary-foreground disabled:opacity-50">
            {busy ? t('settingsPage.plugins.cliInstalling') : t('settingsPage.plugins.cliInstall')}
          </button>
        </div>
      </div>
    </div>
  )
}
```

> 重要：上面 `@bindings/...` 路径和 `OpenDirectoryDialog` 的实际签名需要根据 `frontend/bindings/` 实际生成结果调整。安装实现前先查一次：
> ```bash
> ls /Users/linhuafeng/Work/lemon_tea_desktop/frontend/bindings/
> grep -rn 'OpenDirectoryDialog\|OpenDialog' /Users/linhuafeng/Work/lemon_tea_desktop/frontend/src | head -5
> ```
> 必要时复用现有 mcp 导入流程中的文件夹选择 helper。

`btn-active` / `btn-inactive` 用项目已有的 tailwind 组合类替换；目录选择走 `service/file` 已有的 `OpenDirectoryDialog`（在添加 mcp 工具时已有用例）。

- [ ] **Step 2: 把 modal 接入「插件与工具」页主组件**

找到调用 `<PluginToolList ... onCreate={...} />` 的父组件（设置页主入口）。在 `onCreate('cli')` 时打开 modal，安装成功 callback 时把新 item 推到列表（沿用现有 `ListExtensions` 调用刷新）。

- [ ] **Step 3: TypeScript 编译 + 提交**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop/frontend && npx tsc --noEmit
git add frontend/src/components/settings/plugins/CliInstallModal.tsx frontend/src/<父组件路径>
git commit -m "feat(plugins-ui): add CLI install modal with npm and local source tabs"
```

---

## Task 7: 前端 — CliExtensionDetailView

**Files:**
- Create: `frontend/src/components/settings/plugins/CliExtensionDetailView.tsx`
- Modify: `frontend/src/components/settings/plugins/PluginToolDetailView.tsx`（按 kind 分支渲染）
- Modify: `frontend/src/components/settings/plugins/ExtensionDetailInfo.tsx`（cli kind 字段）

- [ ] **Step 1: 写 `CliExtensionDetailView`**

```tsx
import { useEffect, useState } from 'react'
import { useTranslation } from 'react-i18next'
import { SettingsActionBar } from '@/components/settings/common/SettingsActionBar'
import { SettingsContentLayout } from '@/components/settings/common/SettingsContentLayout'
import { SettingsPanelHeader } from '@/components/settings/common/SettingsPanelHeader'
import { ExtensionDetailInfo } from '@/components/settings/plugins/ExtensionDetailInfo'
import { getJsonError } from '@/components/settings/plugins/jsonError'
import { ResetCliData, UpdateCliManifest } from '@bindings/.../service/plugin'
import type { ExtensionItem } from '@/types/settings'

export function CliExtensionDetailView(props: {
  extension: ExtensionItem
  manifestText: string
  loading: boolean
  onReloaded: (item: ExtensionItem) => void
  onAfterReset: () => void
}) {
  const { t } = useTranslation()
  const [draft, setDraft] = useState(props.manifestText)

  useEffect(() => { setDraft(props.manifestText) }, [props.manifestText, props.extension.id])

  const dirty = draft !== props.manifestText
  const jsonError = getJsonError(draft)
  const valid = jsonError.valid

  const apply = async () => {
    const res = await UpdateCliManifest({ id: props.extension.id, manifest_text: draft })
    if (res?.extension) props.onReloaded(res.extension)
  }

  const reset = async () => {
    if (!confirm(t('settingsPage.plugins.cliResetConfirm'))) return
    await ResetCliData({ id: props.extension.id })
    props.onAfterReset()
  }

  return (
    <SettingsContentLayout
      header={(
        <SettingsPanelHeader
          title={props.extension.name}
          description={props.extension.description || props.extension.version}
          badge={<span className="rounded-full bg-purple-100 px-2.5 py-0.5 text-xs font-medium text-purple-700 dark:bg-purple-500/20 dark:text-purple-400">{t('settingsPage.plugins.kindCli')}</span>}
        />
      )}
      footprint={(
        <SettingsActionBar
          primaryLabel={t('settingsPage.actions.apply')}
          secondaryLabel={t('settingsPage.actions.reset')}
          primaryDisabled={!dirty || props.loading || !valid}
          secondaryDisabled={!dirty || props.loading}
          onPrimaryClick={apply}
          onSecondaryClick={() => setDraft(props.manifestText)}
        />
      )}
    >
      <ExtensionDetailInfo extension={props.extension} draft={draft} onDraftChange={setDraft} />
      <div className="mt-6 flex gap-3 border-t border-border pt-4">
        <button type="button" onClick={reset}
          className="rounded-lg border border-red-300 bg-red-50 px-4 py-2 text-sm text-red-700 hover:bg-red-100 dark:border-red-700 dark:bg-red-500/10 dark:text-red-400">
          {t('settingsPage.plugins.cliResetData')}
        </button>
      </div>
    </SettingsContentLayout>
  )
}
```

- [ ] **Step 2: `PluginToolDetailView` — kind 分支渲染**

在 `PluginToolDetailView` 顶部，当 `props.extension?.kind === 'cli'` 时直接返回 `<CliExtensionDetailView ... />`（其余字段从现有 props 传入）。

- [ ] **Step 3: `ExtensionDetailInfo` 兼容 cli kind**

`ExtensionDetailInfo` 当前展示 mcp 的配置文件路径。CLI 也展示 `ConfigFilePath`（即 manifest 路径）、`RootDir`（即 install dir）。无需新字段，但展示文案的 i18n key 需在 cli 场景下有合理标签：

```tsx
{t('settingsPage.plugins.installDir')}: {extension.root_dir}
{extension.kind === 'cli'
  ? t('settingsPage.plugins.manifestPath')
  : t('settingsPage.plugins.configFilePath')}: {extension.config_file_path}
```

- [ ] **Step 4: TypeScript 编译 + 提交**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop/frontend && npx tsc --noEmit
git add frontend/src/components/settings/plugins/
git commit -m "feat(plugins-ui): add CliExtensionDetailView with manifest editor and reset data"
```

---

## Task 8: i18n + 验收

**Files:**
- Modify: `frontend/src/i18n/locales/en.ts`
- Modify: `frontend/src/i18n/locales/zh-CN.ts`

- [ ] **Step 1: 新增 i18n key（en.ts 与 zh-CN.ts 同步）**

| key | en | zh-CN |
|---|---|---|
| `settingsPage.plugins.addCli` | Add CLI | 添加 CLI |
| `settingsPage.plugins.kindCli` | CLI | CLI |
| `settingsPage.plugins.cliInstallTitle` | Install CLI plugin | 安装 CLI 插件 |
| `settingsPage.plugins.cliFromNpm` | From npm | 从 npm |
| `settingsPage.plugins.cliFromLocal` | From folder | 从本地目录 |
| `settingsPage.plugins.cliNpmPlaceholder` | e.g. @scope/lark-cli | 例如 @scope/lark-cli |
| `settingsPage.plugins.cliLocalPlaceholder` | Pick a folder containing package.json | 选择一个包含 package.json 的目录 |
| `settingsPage.plugins.cliNamePlaceholder` | Display name (defaults to package name) | 显示名（默认使用包名） |
| `settingsPage.plugins.cliInstall` | Install | 安装 |
| `settingsPage.plugins.cliInstalling` | Installing... | 安装中... |
| `settingsPage.plugins.browse` | Browse | 浏览 |
| `settingsPage.plugins.cliResetData` | Reset Data | 重置数据 |
| `settingsPage.plugins.cliResetConfirm` | Clear login state and generated manifest for this plugin? | 清空该插件的登录态与生成的 manifest？ |
| `settingsPage.plugins.installDir` | Install dir | 安装目录 |
| `settingsPage.plugins.manifestPath` | Manifest path | Manifest 路径 |
| `common.cancel` | Cancel | 取消 |

后端错误码文案（`backend/pkg/i18n/`）：

| 错误码 | en | zh-CN |
|---|---|---|
| `ErrCliRuntimeUnavailable` | Plugin runtime not ready; finish onboarding first | 插件运行时尚未就绪，请先完成 onboarding |
| `ErrCliInstallFailed` | CLI install failed | CLI 安装失败 |
| `ErrCliResetDataFailed` | Reset CLI data failed | 重置 CLI 数据失败 |
| `ErrCliManifestInvalid` | Manifest is invalid | Manifest 无效 |
| `ErrCliManifestSaveFailed` | Save manifest failed | 保存 manifest 失败 |

- [ ] **Step 2: TypeScript 编译 + 后端测试**

```bash
cd /Users/linhuafeng/Work/lemon_tea_desktop && go test ./... 2>&1 | grep -E '^(ok|FAIL)' | head -20
cd /Users/linhuafeng/Work/lemon_tea_desktop/frontend && npx tsc --noEmit
```

预期：全 PASS。

- [ ] **Step 3: 提交**

```bash
git add frontend/src/i18n/ backend/pkg/i18n/
git commit -m "feat(i18n): add cli plugin labels and error messages"
```

- [ ] **Step 4: 手工验收**

启动 Wails dev 模式：
1. 打开设置页 → 插件与工具
2. 点「添加 → CLI」，输入一个公开 npm 包名（建议先用一个简单 CLI 如 `@modelcontextprotocol/server-everything` 之类便于验证），点安装
3. 等待安装完成，新条目出现在列表（紫色 CLI 标签）
4. 点中详情面板：能看到 manifest JSON 编辑器
5. 改一行 description，点应用 → manifest.json 落盘、列表项 description 更新
6. 点重置数据 → 二次确认 → `~/.lemontea/plugins/cli_data/<name>/` 被清空
7. 点更多 → 删除 → `~/.lemontea/plugins/cli/<name>/` 被清空，但 `cli_data` 残留（如刚才没重置）

---

## 不在 P3 范围

* AI 自动生成 manifest 草稿（P4）。
* 把 cli 工具注册到 agent 工具池（P4）。
* 登录 / 认证按钮的实际行为（P5）—— 详情页可以预留按钮但目前 disabled，或干脆不显示。
* 聊天端 `install_cli_plugin` meta tool（P6）。
* 安装进度事件 `extension.cli.install.progress`（P3 同步等待，loading spinner 即可；后续可加流式进度）。
* npm 安装失败时的友好错误恢复（本期只展示原始错误信息）。
* Windows 端 CLI 安装 / 调用（本期 macOS / Linux 优先；Windows 留 P6+ 处理 .cmd 包装与 Job Object 容器化）。
